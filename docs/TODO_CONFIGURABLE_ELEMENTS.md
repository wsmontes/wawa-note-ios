# TODO Configurable Elements — Wawa Note

> Documento cumulativo focado na exposição completa de elementos configuráveis via config project.
> 1 tópico por iteração. Meta: 50 tópicos pontiagudos.
> Formato: `[#N] [P?] [Categoria] Descrição` onde P0=crítico, P1=alta, P2=média, P3=baixa.

**Progresso:** 50 / 50 ✅

---

## Categorias

| # | Categoria | Tópicos |
|---|-----------|---------|
| A | Config Project Architecture | 1 |
| B | Provider Configuration | 1 |
| C | Prompt Configuration | 1 |
| D | Settings Configuration | 1 |
| E | Agent Memory Configuration | 1 |
| F | Skills & Lenses Configuration | 1 |
| G | Model & Parameter Configuration | 1 |
| H | Export & Import of Config | 1 |
| I | Manual Editing Journeys | 1 |
| J | AI Editing Journeys | 4 |
| K | Overlap: Manual + AI Editing | 3 |
| L | Partial Config Export/Import | 3 |
| M | Config Validation & Integrity | 5 |
| N | Config Versioning & History | 4 |
| O | Config Sync & Propagation | 5 |
| P | Security of Configurable Elements | 5 |
| Q | User Journeys (Config) | 4 |
| R | Bugs & Defects | 5 |
| S | Logging & Debug | 2 |
| T | Resiliency & Edge Cases | 1 |

---

## A — Config Project Architecture

- [ ] **[#1] [P0] `ConfigProjectService.syncConfigProject()` recria TODOS os items de configuração a cada launch — apaga e reinsere providers, prompts, settings, e memories, perdendo qualquer edição manual feita pelo utilizador nos items do config project.** `ConfigProjectService.swift:65-140` — o método `syncConfigProject` é chamado em cada launch. O fluxo: (1) fetch de todos os items existentes no config project, (2) filtra por prefixos `"Provider: "`, `"Prompt: "`, `"App Settings"`, `"Agent Memories"`, (3) desatacha (`projectID = nil`) e marca para delete (`context.delete`), (4) recria items novos a partir do estado atual da app (SwiftData providers, PromptStore, UserDefaults, AgentMemoryStore). **Problemas:** (a) **Edições manuais são destruídas a cada launch.** Se o utilizador abre o config project, edita o body de "Provider: OpenAI" (ex: muda `defaultModel` de `gpt-5-nano` para `gpt-5`), e faz `context.save()`, a alteração persiste na sessão atual. No próximo launch, `syncConfigProject` APAGA o item editado e recria-o com os valores originais do `AIProviderConfigModel` em SwiftData. O utilizador perde a edição sem aviso. (b) **O config project é write-only do ponto de vista do utilizador.** Qualquer alteração feita via config project (editar um KnowledgeItem) é efetivamente descartável — o sistema nunca LÊ os items do config project para aplicar configurações. O fluxo é unidirecional: estado da app → config project items. Não há fluxo inverso: config project items → estado da app. (c) **`try? context.save()` na linha 139 engole falhas.** Se o save falha após recriar 20 items, o estado fica inconsistente: items antigos foram apagados, novos não foram persistidos. (d) **Detach + delete pattern sem `context.save()` intermédio.** Items são desatachados (`projectID = nil`) e marcados para delete (`context.delete`) em loop, mas o save só acontece no final. Se o save falha, items ficam com `projectID = nil` (órfãos, não aparecem em nenhum projeto) mas não são deletados. (e) **Sem locking ou atomicidade.** Se o utilizador está a editar um item do config project ENQUANTO `syncConfigProject` corre (ex: em background no launch), há race condition. (f) **O config project aparece na lista de projetos?** `isConfigProject` e `isConfigProjectSlug` existem mas não há evidência de que a UI filtra o config project das listas. Se aparecer, o utilizador vê "Wawa Note Config" como um projeto normal. **Correção:** (1) `syncConfigProject` deve fazer diff em vez de delete-all + recreate: comparar items existentes com estado atual, atualizar apenas o que mudou, preservar edições manuais. (2) Implementar fluxo inverso: `applyConfigFromProject()` que lê items do config project e atualiza `AIProviderConfigModel`, `PromptStore`, `UserDefaults`, etc. (3) Adicionar flag `isAutoGenerated: Bool` nos metadados do item para distinguir items gerados pelo sistema de items editados manualmente. (4) UI deve esconder o config project das listas normais de projetos (usar `isConfigProjectSlug` no `InboxView` e `ExploreView`). (5) Adicionar `configSyncLock` (actor ou serial queue) para impedir edição concorrente.

---

## B — Provider Configuration

- [ ] **[#2] [P0] Provider config exposto no config project omite campos críticos e expõe `apiKeyKeychainIdentifier` sem indicar se a key existe — `availableModelsJSON` e `notes` estão ausentes, e o item é read-only (edições não se propagam de volta ao `AIProviderConfigModel`).** `ConfigProjectService.swift:94-109` serializa providers como JSON com 8 campos: `name`, `type`, `defaultModel`, `baseURL`, `supportsStreaming`, `supportsTools`, `supportsAudio`, `supportsEmbeddings`. **Problemas:** (a) **`availableModelsJSON` não é exposto.** O `AIProviderConfigModel` tem `availableModelsJSON: String?` (linha 16) que lista os modelos disponíveis para este provider — mas o config project NÃO inclui este campo. Se o utilizador quer saber que modelos pode usar, tem de ir a Settings > Provider. (b) **`notes` não é exposto.** O campo `notes: String?` (linha 18) permite ao utilizador guardar notas sobre o provider (ex: "Conta pessoal, tier 2") — não aparece no config project. (c) **`apiKeyKeychainIdentifier` é exposto mas sem indicação de presença.** O config JSON mostra `"apiKeyKeychainIdentifier": "com.wawa-note.openai"` — mas não indica se a key EXISTE no Keychain. O utilizador vê o identifier mas não sabe se precisa de adicionar uma key. (d) **`baseURL` é exposto como `Any` com cast frágil.** `"baseURL": config.baseURLString as Any` — se `baseURLString` é nil, o JSON mostra `"baseURL": null`. Se o utilizador editar para uma URL malformada, não há validação. (e) **Editar o provider JSON no config project NÃO atualiza o provider real.** Tal como o problema geral do config project, alterações no body do item "Provider: OpenAI" não se propagam para `AIProviderConfigModel` — o provider continua a usar os valores originais. (f) **Novos providers criados via config project não são registados.** Se o utilizador duplica o item "Provider: OpenAI" e renomeia para "Provider: Azure", o sistema não deteta o novo provider porque `syncConfigProject` só lê providers do SwiftData (`AIProviderConfigModel`), não dos items do config project. (g) **Provider delete não é exposto.** Se o utilizador apaga o item "Provider: OpenAI" do config project, o provider real NÃO é removido do SwiftData. Na próxima sync, o item é recriado. (h) **API keys não são configuráveis via config project de todo.** Correcto do ponto de vista de segurança (keys devem ficar no Keychain), mas o config project deveria indicar "Key status: present" ou "Key status: missing — add in Settings". **Correção:** (1) Expor TODOS os campos do `AIProviderConfigModel` no config project JSON, exceto `apiKeyKeychainIdentifier` (substituir por `keyStatus: "present" | "missing"`). (2) Implementar `applyProviderFromConfig(item: KnowledgeItem)` que lê o JSON do body, valida, e atualiza o `AIProviderConfigModel` correspondente. (3) Suportar criação de novos providers via config project: items com título `"Provider: <name>"` e tag `config` + `provider` que não correspondem a nenhum provider existente devem ser registados. (4) Validar campos do provider no save: `baseURL` deve ser URL válida ou null, `defaultModel` não pode ser vazio, `type` deve ser um `ProviderType` válido.

---

## C — Prompt Configuration

- [ ] **[#3] [P0] Prompts são expostos como items com body em formato híbrido Markdown+JSON — o utilizador tem de editar JSON dentro de Markdown, sem syntax highlighting, sem validação de schema, e sem indicação clara de que campo editar.** `ConfigProjectService.swift:111-122` cria items com título `"Prompt: <name>"` e body composto por duas secções: `# Metadata\n<JSON>\n\n# Content\n<prompt text>`. **Problemas:** (a) **Formato híbrido frágil.** O body mistura JSON (metadados) com texto livre (conteúdo do prompt) separados por headers Markdown. Se o utilizador edita o prompt e acidentalmente apaga o header `# Content`, o sistema não consegue fazer parse para separar metadados de conteúdo. Se adiciona um `#` no texto do prompt, o parser pode confundir com um novo header. (b) **JSON nos metadados sem validação.** Os metadados incluem `name`, `category`, `isUserEdited`, `variables` — mas se o utilizador escreve JSON inválido (ex: `"variables": [rascunho]` sem aspas), o item fica corrupto e o sistema não consegue reimportar. (c) **`isUserEdited` não é respeitado pelo `syncConfigProject`.** O PromptStore marca prompts editados pelo utilizador com `isUserEdited: true` — mas `syncConfigProject` NÃO verifica esta flag e sobrescreve na mesma. (d) **`variables` são expostas como array mas sem explicação.** O campo `variables` lista placeholders como `["transcript", "analysis"]` — mas o utilizador não sabe o que são, como usá-los, ou se pode adicionar novos. (e) **Sem diff visual entre o prompt base e a edição do utilizador.** O `PromptStore` guarda o prompt base (de `ai_config.json`) e os overrides do utilizador (de `prompts.json` em `configsDirectory`), mas o config project só mostra o prompt final (base + overrides merged). O utilizador não sabe que partes foram herdadas e que partes foram editadas. (f) **Prompts novos não podem ser criados.** O utilizador pode duplicar "Prompt: analysis_system" e renomear para "Prompt: my_custom_analysis" — mas o sistema ignora porque só reconhece prompts registados no `PromptStore`. (g) **Sem indicação de que prompts são usados por que features.** O config project não mostra "Este prompt é usado por: ContentPipeline (analysis), Chat (chat_system)". O utilizador edita um prompt sem saber o impacto. **Correção:** (1) Separar metadados e conteúdo em campos distintos do KnowledgeItem: metadados no `bodyText` como JSON puro, conteúdo do prompt como `transcript` ou attachment dedicado. (2) Adicionar validação de schema JSON nos metadados ao guardar. (3) `syncConfigProject` deve verificar `isUserEdited` e pular prompts editados (ou fazer merge em vez de overwrite). (4) Adicionar campo `usage: [String]` aos metadados listando as features que usam este prompt. (5) Suportar criação de novos prompts via config project com registo automático no `PromptStore`. (6) Expor diff visual: mostrar `basePrompt` vs `userOverride` vs `effectivePrompt`.

---

## D — Settings Configuration

- [ ] **[#4] [P1] "App Settings" expõe 25+ chaves de UserDefaults como JSON plano — mas sem documentação do que cada chave faz, sem validação de tipos, e com chaves internas misturadas com configuráveis.** `ConfigProjectService.buildSettingsSnapshot()` (linha 144-189) compila um dicionário com ~25 chaves cobrindo: app info, transcrição, automação, áudio, developer mode, provider ativo, Anarlog, e AI config summary. **Problemas:** (a) **Chaves sem documentação.** O JSON mostra `"voiceProcessing": true` mas o utilizador não sabe que isto corresponde a `audio_raw_mode = false`. A chave no JSON é o inverso da chave no UserDefaults, causando confusão. (b) **Tipos de dados não documentados.** `"autoAnalysisModel": "gpt-5-nano"` — é uma string, mas que valores são válidos? O utilizador não sabe que modelos existem. (c) **Chaves read-only misturadas com configuráveis.** `appVersion`, `buildNumber`, `summaryCacheEntries`, `summaryCacheHitRate` são informativas (não configuráveis) mas aparecem no mesmo JSON que `autoTranscribe` (configurável). O utilizador não sabe quais pode editar. (d) **Editar o JSON não atualiza UserDefaults.** Tal como os outros items do config project, alterações no body de "App Settings" não se propagam para `UserDefaults.standard`. Mudar `"autoTranscribe": false` no JSON não desliga a auto-transcrição. (e) **Valores booleanos representados inconsistentemente.** `UserDefaults.bool(forKey:)` retorna `false` para chaves inexistentes — o JSON pode mostrar `false` para uma chave que nunca foi setada, vs `false` porque o utilizador desligou. (f) **`activeProviderId` exposto como string mas sem validação de que o provider existe.** Se o utilizador muda para `"activeProviderId": "azure"` e não há provider Azure registado, o sistema fica sem provider ativo. (g) **Settings de áudio com nomes confusos.** `voiceProcessing` (invertido de `audio_raw_mode`) e `speakerphoneMode` (invertido? não, é direto) — a lógica de inversão não é documentada. (h) **Chaves da App Group não expostas.** `UserDefaults(suiteName: "group.com.wawa-note")` é usado para comunicação com a Share Extension e Watch, mas estas chaves NÃO aparecem no config project. **Correção:** (1) Adicionar schema documentado: cada chave deve ter `key`, `value`, `type` ("Bool", "String", "Enum:on|off"), `editable: true|false`, `description`. (2) Implementar `applySettingsFromConfig(item:)` que lê o JSON, valida tipos, e escreve em `UserDefaults.standard`. (3) Separar secções: `"readOnly"` (app info, stats) vs `"editable"` (transcrição, automação, áudio). (4) Para chaves booleanas, usar os nomes canónicos do UserDefaults (não inversos). (5) Validar `activeProviderId` contra providers registados. (6) Expor chaves da App Group como secção separada `"appGroup"`.

---

## E — Agent Memory Configuration

- [ ] **[#5] [P1] "Agent Memories" expõe memórias como JSON array sem edição individual — o utilizador vê uma parede de JSON com todas as memórias agregadas, sem poder editar, criar, ou remover memórias específicas.** `ConfigProjectService.swift:131-136` serializa TODAS as memórias do `AgentMemoryStore.shared.listAll()` num único item com body JSON. **Problemas:** (a) **Array monolítico, sem edição por entrada.** O utilizador vê `[{id: ..., key: ..., value: ..., ...}, {id: ..., ...}]` — para editar uma memória, tem de navegar um JSON array potencialmente enorme e encontrar a entrada certa. Se errar uma vírgula ou bracket, o JSON inteiro fica inválido. (b) **Sem criação de novas memórias.** O utilizador pode adicionar um objeto ao array JSON, mas `syncConfigProject` NÃO lê o item "Agent Memories" para aplicar alterações ao `AgentMemoryStore`. A nova memória é perdida no próximo launch. (c) **Sem delete individual.** Remover um objeto do JSON array não remove a memória do store. (d) **Sem indicação de prioridade ou idade.** As memórias no `AgentMemoryStore` têm `createdAt`, `updatedAt`, `priority`, `source` (user vs agent vs system) — mas o JSON exposto não inclui todos estes campos. O utilizador não sabe quais memórias foram criadas pelo agente vs manualmente. (e) **Memórias podem conter dados sensíveis.** O agente pode armazenar trechos de transcrições como memória — expor todas as memórias em texto plano num item do config project pode expor informação confidencial. (f) **Sem search ou filtro.** Com 200+ memórias, o JSON array é ilegível. O utilizador precisa de search (`grep`) para encontrar uma memória específica. (g) **Conflito entre edição manual e agente.** Se o utilizador edita uma memória manualmente e o agente atualiza a mesma memória na mesma sessão, a versão do agente sobrescreve a edição manual (ou vice-versa, dependendo da ordem de save). **Correção:** (1) Em vez de um único item "Agent Memories", criar um item por memória: `"Memory: <key>"` com body JSON individual. (2) Implementar `applyMemoriesFromConfig()` que lê items `"Memory: *"` do config project e faz upsert no `AgentMemoryStore`. (3) Cada item de memória deve incluir metadata completa: `id`, `key`, `value`, `priority`, `source`, `createdAt`, `updatedAt`, `tags`. (4) Adicionar flag `isUserLocked` — se true, o agente não pode modificar esta memória. (5) Expor apenas memórias com `source != "system"` — memórias internas do sistema não devem ser editáveis. (6) Suportar criação de novas memórias: items com título `"Memory: <key>"` e tag `config` + `memory` criam entrada no `AgentMemoryStore`.

---

## F — Skills & Lenses Configuration

- [ ] **[#6] [P1] Skills e Lenses NÃO são expostos como items no config project — o `AnalysisSkillStore` (`skills.json`) e as lenses do `ai_config.json` são ignorados pelo `syncConfigProject`, deixando duas grandes superfícies de configuração inacessíveis.** `ConfigProjectService.syncConfigProject()` (linha 65-140) sincroniza Providers, Prompts, Settings, e Memories — mas NÃO sincroniza Skills (do `AnalysisSkillStore`, persistidas em `configs/skills.json`) nem Lenses (do `AIConfigService.shared.config.lenses`, definidas em `ai_config.json`). **Problemas:** (a) **Skills são uma superfície de configuração invisível.** O `AnalysisSkillStore` gere templates de análise que definem que secções o agente deve produzir (ex: "Key Decisions", "Action Items", "Risks"). Estas skills existem no filesystem (`configs/skills.json`) mas não aparecem no config project. O utilizador não sabe que existem, não pode editá-las, e não pode criar novas. (b) **Lenses são apenas listadas no Settings JSON como contagem.** `buildSettingsSnapshot()` inclui `"aiLenses": [...keys...]` — apenas os nomes, não a configuração completa. Uma lens define: `id`, `name`, `description`, `systemPrompt`, `userPrompt`, `outputSchema`. Nada disto é exposto. (c) **Skills e Lenses têm sobreposição conceptual.** Ambos definem "como analisar" — skills definem a estrutura de output, lenses definem a perspetiva de análise. O utilizador não sabe a diferença porque nenhum é exposto. (d) **`ai_config.json` é a fonte de verdade para lenses mas é read-only.** O ficheiro está em `Bundle.main` — não pode ser editado em runtime. As lenses deveriam ser sobreponíveis via `configs/lenses.json` (padrão igual ao `PromptStore`), mas este mecanismo não existe. (e) **Skills não têm UI de edição.** O `AnalysisSkillStore` tem métodos `add`, `update`, `remove` — mas não são expostos nem no Settings UI nem no config project. (f) **Framework schemas de projeto também não estão expostos.** `Project.frameworkJSON` define o schema de análise específico do projeto — relacionado com skills/lenses mas armazenado num atributo SwiftData, não no config project. **Correção:** (1) Adicionar items `"Skill: <name>"` e `"Lens: <name>"` ao config project durante `syncConfigProject`. (2) Implementar `applySkillsFromConfig()` e `applyLensesFromConfig()` com update bidirecional. (3) Skills devem ser expostas como JSON schema completo com `sections`, `promptTemplate`, `category`. (4) Lenses devem ser expostas com `systemPrompt`, `userPrompt`, `outputSchema`, `modelPreference`. (5) Implementar `configs/lenses.json` como override file (padrão PromptStore) para permitir edição de lenses em runtime. (6) Adicionar relação bidirecional: Skills/Lenses ↔ Framework Schemas de projeto, com indicação de "inherited from skill X, overridden by project Y".

---

## G — Model & Parameter Configuration

- [ ] **[#7] [P1] Parâmetros de modelo (`temperature`, `maxTokens`, `modelPresets`) são definidos em `ai_config.json` e `AIConfigService` mas NÃO são expostos como items editáveis no config project — apenas aparecem como contagens no Settings JSON.** `ConfigProjectService.buildSettingsSnapshot()` (linha 177-181) inclui `aiModelPresets` (contagem) e `aiFeatures` (lista de nomes) — mas não o conteúdo. `AIConfigService.shared.config.modelPresets` define parâmetros como `temperature`, `maxTokens`, `responseFormat` por modelo — inacessíveis. **Problemas:** (a) **Model presets são invisíveis.** O `ai_config.json` define presets como `"gpt-5-nano": {temperature: 0.4, maxTokens: 4096}`. O utilizador não sabe que parâmetros cada modelo usa, e não pode ajustá-los. (b) **`requestParams(for:model:)` resolve parâmetros com lógica complexa não documentada.** O método em `AIConfigService` aplica: feature config → model preset cap → reasoning model detection (temperature → nil). Esta lógica é uma caixa negra para o utilizador. (c) **Sem override de parâmetros por feature.** O `ai_config.json` tem `features: {analysis: {temperature: 0.4, maxTokens: 8000}}` — mas o config project não expõe estas definições. (d) **Parâmetros de reasoning models são tratados com exceções hardcoded.** Se o modelo é detetado como "reasoning" (ex: `o1`, `o3`, `gpt-5-reasoning`), `temperature` é forçado a nil. A lista de modelos reasoning é hardcoded — novos modelos não serão reconhecidos. (e) **`maxTokens` sem validação contra o context window do modelo.** O preset define `maxTokens: 8000` mas o modelo pode ter context window de 4096 — o pedido falha na API sem validação prévia. (f) **Sem exposição de parâmetros avançados:** `topP`, `frequencyPenalty`, `presencePenalty`, `stop` — o `AIRequest` suporta estes campos mas não são configuráveis via `ai_config.json` nem expostos no config project. **Correção:** (1) Adicionar item `"Model Presets"` ao config project com JSON completo de todos os presets, incluindo `temperature`, `maxTokens`, `responseFormat`, `topP`. (2) Adicionar items `"Feature: <name>"` com a configuração de parâmetros por feature. (3) Documentar a lógica de `requestParams` num campo `"resolutionLogic"` no item. (4) Implementar `configs/model_presets.json` como override (padrão PromptStore) para permitir edição de presets em runtime sem modificar o Bundle. (5) Validar `maxTokens` contra o context window do modelo (usando `AIConfigService.contextWindow(for:)`). (6) Expor reasoning model detection como configuração editável: `reasoningModels: ["o1", "o3", "gpt-5-reasoning"]`.

---

## H — Export & Import of Config

- [ ] **[#8] [P1] `InstanceExportService.exportComplete()` exporta prompts, memories, e AI config em JSON — mas não exporta providers (com API keys), skills, ou UserDefaults. E NÃO EXISTE `importConfig()` para reimportar.** O export completo (Settings → Export Complete JSON) inclui `prompts`, `memories`, e `aiConfigSummary` (contagens) — mas não o config completo. **Problemas:** (a) **Providers não são exportados.** `AIProviderConfigModel` contém `name`, `type`, `baseURL`, `defaultModel`, `availableModelsJSON`, `notes` — mas `exportComplete` não os inclui. E corretamente NÃO exporta API keys (Keychain). Mas sem providers, o import noutro dispositivo requer reconfigurar todos os providers manualmente. (b) **Skills não são exportadas.** `AnalysisSkillStore` (`configs/skills.json`) não é incluído no export. (c) **UserDefaults completo não é exportado.** Só um subconjunto aparece no Settings JSON — chaves como `transcription_mode`, `automation_auto_transcribe` são exportadas, mas outras como `audio_raw_mode`, chaves da App Group, e flags internas (`hasLaunchedBefore`) não. (d) **NÃO EXISTE `importConfig()`.** `InstanceExportService` só exporta — não há `InstanceImportService`. O JSON exportado não pode ser reimportado para restaurar ou migrar configurações. (e) **Export de config project items vs export de config real.** O config project tem items (`Provider: X`, `Prompt: X`, etc.) — mas estes items são o reflexo do estado, não o estado em si. Exportar o config project como um projeto normal exporta KnowledgeItems, não a configuração real. (f) **Sem export parcial.** O utilizador não pode exportar só "Prompts" ou só "Provider OpenAI". O export é tudo-ou-nada via Settings. (g) **Sem migração cross-device de configuração.** O utilizador configura providers, prompts, skills, settings no iPhone — não há como transferir para o iPad sem reconfigurar manualmente. **Correção:** (1) Implementar `ConfigExportService.exportFull(fileStore:)` que serializa TODAS as superfícies de configuração num bundle `.wawaconfig`: `providers.json` (sem API keys), `prompts.json`, `settings.json`, `skills.json`, `lenses.json`, `memories.json`, `model_presets.json`. (2) Implementar `ConfigImportService.importFull(bundleURL:)` que lê o bundle e aplica cada ficheiro ao respetivo store. (3) Provider export deve incluir tudo exceto `apiKeyKeychainIdentifier` — o import deve criar providers com key status "missing" e instruir o utilizador a adicionar keys. (4) Suportar export/import parcial: `exportPromptsOnly()`, `exportProvidersOnly()`, `exportSettingsOnly()`. (5) Adicionar opção "Export Configuration" no Settings (ao lado do "Export Complete JSON") que gera o bundle `.wawaconfig`. (6) O config project deve ter opção "Export Config" e "Import Config" no menu de ações do projeto.

---

## I — Manual Editing Journeys

- [ ] **[#9] [P0] O utilizador que edita manualmente um item do config project (ex: Prompt JSON) NÃO TEM feedback sobre se a edição foi aplicada — o sistema não lê items do config project, portanto a edição parece ter funcionado (o item foi guardado) mas não tem efeito real.** O user journey de edição manual: (1) utilizador abre config project, (2) encontra "Prompt: analysis_system", (3) edita o body (muda o texto do prompt), (4) faz save (context.save), (5) vê o item atualizado — aparente sucesso. Mas (6) `syncConfigProject` no próximo launch APAGA a edição. E mesmo antes do relaunch, (7) o sistema NÃO lê o item para atualizar o `PromptStore` — o prompt editado nunca é usado. **Problemas:** (a) **Falsa confirmação de sucesso.** A UI do KnowledgeDetail mostra o item editado como "Updated" — mas o sistema ignora a edição. O utilizador não recebe nenhum aviso "Changes to config project items must be applied via Settings". (b) **Sem indicação de que items são "read-only mirror".** O config project parece um projeto normal — items têm título, body, tags, podem ser editados. Nada indica que são um espelho do estado do sistema. (c) **Sem validação de edições manuais.** Se o utilizador edita "Provider: OpenAI" e escreve JSON inválido, o item guarda — mas na próxima sync, o item é apagado e recriado com JSON válido. O utilizador não sabe que o JSON estava inválido. (d) **Sem preview do efeito da edição.** "Se eu mudar `autoTranscribe: false`, o que acontece?" — sem tooltip, sem documentação inline. (e) **Edição manual em conflito com sync automático.** Se o utilizador edita um item e, ao mesmo tempo, o sistema atualiza o estado (ex: provider é modificado via Settings), o `syncConfigProject` não faz merge — simplesmente apaga e recria. **Correção:** (1) Cada item do config project deve ter banner "⚠️ This is a read-only mirror. Changes made here will be lost on next launch. To edit configuration, use Settings or enable Config Editing mode." (2) Implementar modo "Config Editing": toggle que desliga `syncConfigProject` automático e permite edição manual com `applyConfigFromProject()` manual. (3) Adicionar validação inline durante edição: JSON schema validation, type checking, referential integrity. (4) Mostrar "Last synced: X minutes ago" e "Next sync: on launch" em cada item. (5) Adicionar botão "Apply Now" que chama `applyConfigFromProject()` para o item específico, com preview das alterações.

---

## J — AI Editing Journeys

- [ ] **[#10] [P1] O agente (AgentLoop) pode LISTAR ficheiros do config project via VFS mas NÃO pode LER a maioria deles — `VFSService.readConfigProjectFile` só implementa leitura para providers e settings JSON, deixando prompts, memórias, e skills inacessíveis ao agente.** `VFSService.swift:1387-1439` — o método `readConfigProjectFile(filename:)` tem um `switch` sobre padrões de nome de ficheiro: `"provider-"` → lê provider JSON, `"settings-"` → lê settings JSON via `buildSettingsSnapshot()`. **Problemas:** (a) **Prompts são listados mas não podem ser lidos.** `VFSService.configPromptNodes` (linha ~1350) cria VFS nodes para cada prompt do `PromptStore.shared.prompts(in: nil)`, com `fileExtension: "md"`. Estes nodes aparecem na listagem do agente como ficheiros `.md` — mas `readConfigProjectFile` NÃO tem um case para ficheiros `.md` ou com prefixo `"prompt-"`. O agente vê o ficheiro, tenta lê-lo, e recebe `nil`. (b) **Agent memories são listadas mas não podem ser lidas.** `VFSService.configMemoryNodes` (linha ~1360) lista `"memory-\(index).json"` para cada memória — mas `readConfigProjectFile` não tem case para `"memory-"`. (c) **Skills e lenses não são listadas NEM lidas.** O VFS não tem nodes para skills do `AnalysisSkillStore` nem para lenses do `AIConfigService`. O agente simplesmente não sabe que existem. (d) **Agente não pode modificar NENHUM ficheiro de config.** Mesmo que pudesse ler, não há `writeConfigProjectFile` — o VFS é read-only para o config project. O agente não pode ajudar o utilizador a afinar um prompt ou adicionar uma memória. (e) **Assimetria entre o que o utilizador vê e o que o agente vê.** O utilizador abre o config project e vê items com body JSON + Markdown. O agente, via VFS, vê ficheiros planos com nomes diferentes — a correspondência não é óbvia. (f) **Se o agente usa `GetItem`/`ListItems` tools em vez de VFS, enfrenta o problema inverso** — pode ler o body do KnowledgeItem mas não sabe que é um espelho do estado real, e edições via tool não se propagam. **Correção:** (1) Implementar leitura completa no `readConfigProjectFile` para TODOS os tipos: prompts (extrair conteúdo do `PromptStore`), memórias (serializar `AgentMemoryStore.shared.listAll()`), skills (serializar `AnalysisSkillStore.shared.allSkills`), lenses (serializar `AIConfigService.shared.config.lenses`). (2) Implementar `writeConfigProjectFile` para permitir que o agente modifique configuração com confirmação do utilizador: escrever no `PromptStore` (via `addOrUpdate`), `AgentMemoryStore` (via `set`), `AnalysisSkillStore` (via `addOrUpdate`). (3) Adicionar VFS nodes para skills e lenses que atualmente não são listados. (4) Alinhar nomes de ficheiros VFS com os títulos dos KnowledgeItems no config project para que utilizador e agente vejam a mesma estrutura. (5) Adicionar capability flag `"config_read"` e `"config_write"` no `AgentToolRegistry` para que tools possam declarar se acedem ao config project.

- [ ] **[#11] [P1] Agent tools (`GetItem`, `ListItems`, `UpdateItem`) operam sobre KnowledgeItems do config project sem saber que são itens de configuração — o agente pode "editar" um provider ou prompt, ver o item atualizado, mas a alteração NÃO se propaga para o estado real do sistema.** `AgentToolRegistry` regista tools como `GetItemTool`, `ListItemsTool`, `UpdateItemTool` que operam sobre `KnowledgeItem` via SwiftData. **Problemas:** (a) **`UpdateItemTool` permite editar qualquer KnowledgeItem**, incluindo items do config project. Se o agente atualiza o body de "Provider: OpenAI", o SwiftData persiste a alteração — mas o `AIProviderConfigModel` real não é atualizado, e o provider continua a usar os valores originais. (b) **`GetItemTool` não indica que o item é do config project.** O agente lê o body JSON e interpreta-o como estado real — não sabe que é um espelho que pode estar desatualizado. (c) **`ListItemsTool` pode listar items do config project se o agente faz query por `projectId`.** Se o agente descobre o ID do config project (via `ListProjects` tool), pode listar todos os items de configuração — mas não tem como saber que são read-only mirrors. (d) **Não há tool dedicada para "aplica esta alteração de config".** Mesmo que o agente queira fazer a coisa certa — editar o item E propagar para o store real — não existe tool para isso. (e) **Agente pode apagar items do config project.** `DeleteItemTool` permite apagar qualquer KnowledgeItem. Se o agente apaga "Provider: OpenAI", o item desaparece — mas na próxima sync, é recriado. Confusão para o agente e utilizador. **Correção:** (1) Todas as tools que operam sobre KnowledgeItems devem verificar `ConfigProjectService.isConfigProject(item.projectID)` e, se true, adicionar um aviso ao output: "⚠️ This item is a configuration mirror. Edits to this item do not affect system configuration. Use Settings to change configuration." (2) Implementar `ApplyConfigTool` que lê um item do config project, valida o JSON, e aplica as alterações ao store correspondente (provider → `AIProviderConfigModel`, prompt → `PromptStore`, etc.). (3) `DeleteItemTool` deve bloquear deleção de items do config project a menos que o utilizador confirme explicitamente. (4) `ListItemsTool` e `GetItemTool` devem incluir metadata `"isConfigItem": true` e `"syncStatus": "mirror"` nos resultados quando o item pertence ao config project.

- [ ] **[#12] [P2] O agente NÃO TEM tools para modificar `PromptStore`, `AgentMemoryStore`, ou `AnalysisSkillStore` diretamente — não pode criar prompts, adicionar memórias, ou registar skills sem passar pelo config project (que é write-only).** O `AgentToolRegistry` (`AgentToolRegistry.swift`) regista ~13 tools: `GetItem`, `ListItems`, `SearchKnowledge`, `GraphAndTaskTools`, `CalendarTools`, `FileTools`, etc. — mas nenhuma tool opera sobre os stores de configuração. **Problemas:** (a) **Agente não pode adicionar uma memória.** Se o agente descobre um padrão (ex: "o utilizador prefere respostas curtas"), não pode chamar `AgentMemoryStore.shared.set(key:value:)` para persistir a preferência. (b) **Agente não pode criar um prompt customizado.** Se o utilizador pede "cria um prompt novo para analisar contratos", o agente pode escrever o texto do prompt mas não pode registá-lo no `PromptStore`. (c) **Agente não pode criar uma skill.** Se o agente sugere uma nova estrutura de análise, não pode adicioná-la ao `AnalysisSkillStore`. (d) **Agente não pode listar providers disponíveis para sugerir fallback.** Não há tool que devolva a lista de `AIProviderConfigModel` com capabilities, health status, e modelos disponíveis. O agente não sabe que providers existem. (e) **Agente não pode testar conectividade de um provider.** Não há tool equivalente a `ProviderConnectViewModel.testConnection()`. Se o utilizador diz "o agente está lento", o agente não pode diagnosticar se é um problema de rede ou de modelo. (f) **Agente não pode alterar settings de automação.** Não há tool para modificar `AutomationSettings.shared.autoTranscribe` ou `autoAnalysisModel`. **Correção:** (1) Adicionar `MemoryTool`: `set_memory(key, value, priority)`, `get_memory(key)`, `list_memories(filter)`, `delete_memory(key)` — wrappers sobre `AgentMemoryStore`. (2) Adicionar `PromptTool`: `get_prompt(name)`, `update_prompt(name, content)`, `reset_prompt(name)` — wrappers sobre `PromptStore`. (3) Adicionar `ProviderInfoTool`: `list_providers()`, `get_provider(id)`, `test_provider(id)` — expõe `AIProviderConfigModel` + health status. (4) Adicionar `SettingsTool`: `get_setting(key)`, `update_setting(key, value)` — opera sobre `AutomationSettings` + `UserDefaults` selecionadas. (5) Adicionar `SkillTool`: `list_skills()`, `get_skill(name)`, `update_skill(name, schema)` — wrappers sobre `AnalysisSkillStore`. (6) Todas as tools de config devem declarar `requiresConfirmation: true` para operações de escrita.

- [ ] **[#13] [P2] O config project NÃO é filtrado das listagens de projetos em tools do agente — `ListProjectsTool` e `GetProjectTool` podem expor o config project como um projeto normal, e o agente pode tentar operar sobre ele sem saber que é especial.** `ConfigProjectService.isConfigProjectSlug` é `"wawa-note-config"` e `isConfigProject` verifica `project.slug == isConfigProjectSlug`. **Problemas:** (a) **`ListProjectsTool` não filtra o config project.** Dependendo da implementação da tool, se faz `FetchDescriptor<Project>` sem predicate que exclua `slug == "wawa-note-config"`, o config project aparece na lista. O agente vê "Wawa Note Config" como um projeto normal. (b) **`GetProjectTool` permite aceder ao config project por ID.** Se o agente obtém o ID do config project (via listagem ou inferência), pode ler todos os items de configuração como se fossem conteúdo normal. (c) **Agente pode tentar adicionar items ao config project.** Se o agente decide "vou organizar este projeto" e adiciona notas ou tarefas ao config project, esses items seriam apagados na próxima sync (porque `syncConfigProject` apaga todos os items com prefixos conhecidos — mas items sem prefixo sobreviveriam, poluindo o config project). (d) **Agente pode tentar renomear o config project.** Se o agente muda o nome de "Wawa Note Config" para outra coisa, a verificação `isConfigProject` por slug ainda funciona — mas o utilizador fica confuso. (e) **Não há tooltip ou aviso no agent loop sobre o config project.** Quando o agente recebe o system prompt, não é informado de que existe um projeto especial de configuração que não deve ser modificado. **Correção:** (1) `ListProjectsTool` deve filtrar `slug != "wawa-note-config"` por padrão, com opção `includeConfig: true` para expor o config project explicitamente. (2) `GetProjectTool` deve incluir flag `"isConfigProject": true` na resposta quando o projeto é o config project. (3) `CreateItemTool` deve rejeitar criações com `projectID` que pertence ao config project, a menos que o item tenha tag `"config"`. (4) Adicionar ao system prompt do agente: "The Wawa Note Config project contains configuration mirrors. Do not modify its items directly. Use Settings or dedicated config tools instead." (5) `UpdateProjectTool` deve bloquear renomeação ou alteração de metadata do config project.

---

## K — Overlap: Manual + AI Editing

- [ ] **[#14] [P1] Conflito entre edições do agente e `syncConfigProject` — se o agente modifica um item do config project via tool e o utilizador faz launch da app, o `syncConfigProject` APAGA a edição do agente sem warning, merge, ou notificação.** O cenário: (1) utilizador pede ao agente "ajusta o prompt de análise para ser mais conciso", (2) agente usa `UpdateItemTool` para modificar o body de "Prompt: analysis_system", (3) SwiftData persiste a alteração — o item mostra o novo texto, (4) utilizador fecha e reabre a app, (5) `ContentView.onAppear` chama `ConfigProjectService.syncConfigProject(context:)`, (6) `syncConfigProject` apaga o item editado e recria-o com o conteúdo original do `PromptStore`. **Problemas:** (a) **Sem distinção entre edição manual e edição do agente.** O sistema não sabe se o item foi modificado pelo utilizador (via UI) ou pelo agente (via tool) — em ambos os casos, a edição é perdida. Mas o agente pode fazer dezenas de edições numa sessão, amplificando o problema. (b) **Agente pode fazer alterações em cascata.** Se o agente edita 5 prompts, 3 memórias, e 2 settings — todas são perdidas no próximo launch. O utilizador pode nem saber que o agente fez essas alterações. (c) **Sem locking ou notificação de conflito.** Não há mecanismo que impeça o agente de editar um item enquanto o `syncConfigProject` está pendente para o próximo launch. (d) **Agente não pode forçar uma sync imediata.** Se o agente edita um prompt, não há tool para dizer "aplica esta alteração agora ao `PromptStore` real". A alteração vive apenas no KnowledgeItem e morre no próximo launch. (e) **O agente pode dar falsa confirmação ao utilizador.** "Pronto, ajustei o prompt de análise" — o utilizador testa e vê que o prompt NÃO mudou (porque o `PromptStore` não foi atualizado), gerando desconfiança no agente. **Correção:** (1) `UpdateItemTool` deve detetar items do config project e, em vez de editar o KnowledgeItem diretamente, chamar a store correspondente: se o item é "Prompt: X" → `PromptStore.shared.addOrUpdate(...)`, se é "Memory: X" → `AgentMemoryStore.shared.set(...)`. Isto garante que edições do agente se propagam imediatamente. (2) Adicionar hook `onConfigItemModified` que notifica `ConfigProjectService` para fazer sync incremental (apenas o item modificado) em vez de full sync no próximo launch. (3) `ConfigProjectService` deve registar um `lastSyncTimestamp` e, no próximo launch, verificar `updatedAt` de cada item — se `updatedAt > lastSyncTimestamp`, preservar a edição e fazer merge com o estado atual. (4) Adicionar tool `SyncConfigNow` que força propagação de todas as edições pendentes do config project para os stores reais.

- [ ] **[#15] [P2] Sem estratégia de merge quando utilizador e agente modificam o mesmo item de configuração — last write wins sem three-way merge, sem conflito detetável, e sem histórico de alterações.** Cenário: (1) utilizador edita "Prompt: analysis_system" manualmente no config project (muda a introdução), (2) agente, na mesma sessão, também edita o mesmo prompt (muda a conclusão), (3) o segundo save sobrescreve o primeiro — uma das edições é perdida silenciosamente. **Problemas:** (a) **SwiftData não tem merge automático.** `context.save()` é um overwrite completo do objeto — não há delta ou diff. O último a gravar ganha. (b) **Sem deteção de conflito.** Não há `version` ou `etag` no `KnowledgeItem` que permita detetar "este item foi modificado desde que o leste". (c) **Sem three-way merge.** Mesmo que o conflito fosse detetado, não há lógica para fazer merge: comparar base (estado original do `PromptStore`) vs edit A (utilizador) vs edit B (agente) e produzir um resultado merged. (d) **Edições concorrentes em stores diferentes.** Se o utilizador edita um provider via Settings UI e o agente edita o mesmo provider via config project — a edição do Settings UI ganha (porque o `AIProviderConfigModel` é a fonte de verdade), mas o agente não sabe que a sua edição foi ignorada. (e) **Sem notificação de "outdated".** Se o agente lê um item, o utilizador edita-o manualmente, e depois o agente tenta escrever — o agente não recebe um erro de "item was modified since you read it". **Correção:** (1) Adicionar campo `configVersion: Int` ao `KnowledgeItem` (ou metadata JSON) incrementado a cada save. Tools de leitura devolvem a versão atual; tools de escrita devem passar a versão que leram — se não corresponder, erro de conflito. (2) Implementar `ConfigMergeService` com estratégia por tipo: para prompts (texto), merge com diff3 (base, ours, theirs); para providers (JSON), merge campo a campo com preferência por último editor; para settings (key-value), last-write-wins com log. (3) Expor conflitos na UI: banner no KnowledgeDetailView "⚠️ This item was modified by the agent while you were editing. [Review Changes]". (4) Adicionar `modifiedBy` metadata ("user" | "agent" | "system") a cada item para atribuição de culpa em conflitos.

- [ ] **[#16] [P1] `isUserEdited` flag no `PromptStore` e `AnalysisSkillStore` existe mas NÃO é respeitada pelo `syncConfigProject` — prompts e skills editados pelo utilizador são sobrescritos na mesma pelo sync, e a flag é exposta no JSON mas ignorada na sincronização.** `PromptStore` (`ContentPipelineService.swift:1141-1303`) marca prompts com `isUserEdited: true` quando o utilizador modifica um prompt (via `addOrUpdate`). `AnalysisSkillStore` (`AnalysisSkillStore.swift:155-165`) faz o mesmo. O `syncConfigProject` (`ConfigProjectService.swift:111-122`) expõe `"isUserEdited": true` no JSON de metadados do prompt. **Problemas:** (a) **`syncConfigProject` não verifica `isUserEdited` antes de apagar e recriar.** O método `syncConfigProject` (linha 72-82) apaga TODOS os items existentes com prefixos conhecidos, incluindo prompts com `isUserEdited: true`. Depois recria-os a partir do `PromptStore.shared.prompts(in: nil)` — que já inclui os overrides do utilizador, portanto o conteúdo está correto, mas o KnowledgeItem é recriado (novo `id`, novo `createdAt`). (b) **Perda de metadata do KnowledgeItem.** Se o utilizador adicionou tags, anotações, ou attachments ao item "Prompt: X" no config project, essas são perdidas porque o item é apagado e recriado. (c) **`isUserEdited` não é respeitado no flow inverso (apply).** Se o utilizador edita o JSON de um prompt no config project e faz "Apply", o sistema deveria marcar `isUserEdited: true` no `PromptStore` — mas como não há flow inverso, isto nunca acontece. (d) **Skills têm o mesmo problema.** `AnalysisSkillStore.skills[name].isUserEdited` existe mas `syncConfigProject` nem sequer sincroniza skills (ver #6). (e) **Reset de skill não funciona corretamente para skills criadas pelo utilizador.** `resetSkill(named:)` (`AnalysisSkillStore.swift:208-213`) define `isUserEdited = false` e recarrega built-ins — mas se a skill não existe nos built-ins (foi criada pelo utilizador), ela simplesmente desaparece. **Correção:** (1) `syncConfigProject` deve fazer diff em vez de delete-all: para cada prompt, verificar se o KnowledgeItem existe, comparar `isUserEdited`, e apenas atualizar se `isUserEdited == false` OU se o conteúdo no `PromptStore` mudou desde a última sync. (2) Preservar `id`, `createdAt`, tags, e attachments do KnowledgeItem original quando o conteúdo não mudou. (3) Implementar `applyPromptFromConfig(item:)` que lê o JSON do item, atualiza o `PromptStore`, e marca `isUserEdited: true`. (4) `resetSkill` deve verificar se a skill existe nos built-ins; se não, remover do store em vez de deixar órfã.

---

## L — Partial Config Export/Import

- [ ] **[#17] [P1] Não existe export parcial de configuração — o utilizador não pode exportar só "Prompts" ou só "Provider OpenAI" ou só "Settings de Automação". O export é tudo-ou-nada via "Export Complete JSON" nos Settings.** `InstanceExportService` (`ProjectExportService.swift`) tem `exportComplete()` que exporta tudo: `ConfigExport`, `AnalysisExport`, `ContentExport`, `GraphExport`. **Problemas:** (a) **Export completo é demasiado pesado para partilha rápida.** Se o utilizador quer partilhar apenas o prompt de análise com um colega, tem de exportar o JSON completo (que inclui todas as análises, grafos, items, etc.) e o colega tem de extrair manualmente a secção relevante. (b) **Sem export granular por tipo.** Não há `exportPromptsOnly()`, `exportProvidersOnly()`, `exportSettingsOnly()`, `exportSkillsOnly()`. (c) **Sem export por item individual.** O utilizador não pode fazer "Export this prompt" no KnowledgeDetailView de um item do config project. (d) **Sem preview do que vai ser exportado.** O utilizador carrega em "Export Complete JSON" e recebe um ficheiro — sem saber o tamanho, quantos items inclui, ou que dados sensíveis podem estar expostos. (e) **Sem opção "Export for Sharing" que remove dados pessoais.** O export inclui transcrições, análises, e metadados de reuniões — informação potencialmente confidencial. **Correção:** (1) Implementar `ConfigExportService` com métodos granulares: `exportPrompts()`, `exportProvider(id:)`, `exportSkills()`, `exportMemories()`, `exportSettings()`, `exportAllConfig()`. (2) Adicionar botão "Export" no KnowledgeDetailView para items do config project, que gera JSON apenas desse item. (3) Adicionar UI de export no Settings com checkboxes: ☑ Prompts (3) ☑ Providers (2) ☑ Skills (7) ☑ Memories (45) ☑ Settings (25 keys) — com contagem ao vivo. (4) Adicionar opção "Anonymize" que substitui nomes de reuniões, títulos, e conteúdo por placeholders. (5) Mostrar preview do JSON antes de partilhar, com tamanho estimado e contagem de items.

- [ ] **[#18] [P1] `ConfigExport` em `InstanceExportService.exportComplete()` tem `activeProviderID: nil` e `preferredModel: nil` sempre — dois campos críticos do export de configuração são sempre nil.** `ProjectExportService.swift:753-758` — o método `exportConfig()` cria `ConfigExport(activeProviderID: nil, preferredModel: nil, ...)`. **Problemas:** (a) **`activeProviderID` está disponível mas não é exportado.** `ActiveProviderManager.shared.getActiveProviderID()` retorna o ID do provider ativo — mas `exportConfig()` não o chama. O campo é simplesmente hardcoded a `nil`. (b) **`preferredModel` está disponível mas não é exportado.** `AutomationSettings.shared.autoAnalysisModel` contém o modelo preferido para análise — mas não é incluído no export. (c) **Quem importa o JSON não sabe que provider estava ativo.** Se o utilizador exporta a configuração para migrar para outro dispositivo, o novo dispositivo não sabe que provider usar como padrão. (d) **Os campos existem no modelo `ConfigExport`** (definido algures como `activeProviderID: String?` e `preferredModel: String?`) — são opcionais, mas deviam ser preenchidos quando a informação existe. (e) **Outros campos do `ConfigExport` podem estar corretos** (`autoTranscribe`, `autoAnalyze`, `autoAnalysisModel`, `prompts`, `memories` são exportados) — mas a omissão do `activeProviderID` é inconsistente. **Correção:** (1) Preencher `activeProviderID` com `ActiveProviderManager.shared.getActiveProviderID()`. (2) Preencher `preferredModel` com `AutomationSettings.shared.autoAnalysisModel` (ou o modelo do provider ativo). (3) Adicionar `activeProviderName` e `activeProviderType` para contexto humano. (4) Adicionar teste unitário que verifica que `ConfigExport` não tem campos nil quando há providers configurados.

- [ ] **[#19] [P1] NÃO EXISTE `InstanceImportService` — o JSON exportado via "Export Complete JSON" ou "Export Configuration" não pode ser reimportado para restaurar ou migrar configuração entre dispositivos.** `InstanceExportService` é a única direção implementada. Não há `InstanceImportService`, `ConfigImportService`, ou qualquer mecanismo de importação de configuração a partir de JSON. **Problemas:** (a) **Migração cross-device impossível.** O utilizador configura 3 providers, 5 prompts customizados, 45 memórias, e 7 skills no iPhone — não há como transferir para um iPad sem reconfigurar manualmente. (b) **Backup de configuração é só de ida.** O utilizador pode exportar a configuração para backup, mas nunca a pode restaurar. Se perder o dispositivo, perde a configuração. (c) **Formato de export não é documentado.** O JSON exportado tem uma estrutura específica (`ConfigExport`, `AnalysisExport`, etc.) mas não há schema documentado que um utilizador avançado possa usar para criar configuração offline. (d) **Provider export não inclui API keys (correto por segurança).** Mas na importação, o sistema deveria: (1) criar os providers com `apiKeyKeychainIdentifier: nil`, (2) marcar como "key missing", (3) mostrar banner "This provider needs an API key. Tap to add." (e) **Sem validação de integridade na importação.** Se o JSON de import tem providers que referenciam modelos que não existem, ou prompts com variáveis inválidas, não há validação pré-importação. (f) **Sem dry-run de importação.** O utilizador não pode pré-visualizar "este ficheiro vai criar 2 providers, 5 prompts, 3 skills" antes de confirmar. **Correção:** (1) Implementar `InstanceImportService.importComplete(json:)` que lê o JSON exportado e cria/atualiza providers, prompts, settings, skills, e memórias. (2) Usar upsert (não delete+recreate): comparar por `id` ou `name`, atualizar se existe, criar se não. (3) Provider import: criar `AIProviderConfigModel` sem API key, mostrar badge "Needs Key" no Settings. (4) Adicionar validação pré-importação: verificar schemas JSON, integridade referencial, conflitos com configuração existente. (5) Adicionar preview UI: "This import contains: 2 providers (1 new, 1 update), 5 prompts (3 new, 2 conflicts), …". (6) Suportar import de bundle `.wawaconfig` (zip com múltiplos JSON) para export/import completo.

---

## M — Config Validation & Integrity

- [ ] **[#20] [P1] `ai_config.json` não tem validação de schema ao carregar — JSON malformado, campos obrigatórios em falta, ou tipos errados causam fallback silencioso para config vazia, e a app arranca com funcionalidade severamente degradada sem o utilizador saber.** `AIConfigService.swift:77-99` — o `init()` tenta carregar `ai_config.json` do bundle. Se `Data(contentsOf:)` falha (ficheiro não encontrado) ou `JSONDecoder().decode` falha (JSON inválido), o erro é logado e o `config` é inicializado com `AIConfig.defaultValue` — que tem `providers: [:]` vazio e `features: [:]` vazio. **Problemas:** (a) **Degradação silenciosa.** Com `providers: [:]` vazio, `ProviderTemplates.all` retorna `[]` — o Settings UI não mostra templates de providers. O utilizador não pode adicionar providers porque a lista de templates está vazia. Mas a app não crasha — simplesmente parece não suportar providers. (b) **Funcionalidades dependentes de `features` quebram.** `features.analysis`, `features.chat`, `features.summarization` definem parâmetros como `systemPrompt`, `userPrompt`, `temperature`, `maxTokens`. Sem elas, o agente e o pipeline de conteúdo usam fallbacks hardcoded que podem não ser adequados. (c) **Sem distinção entre "ficheiro não encontrado" e "JSON inválido".** Ambos produzem o mesmo fallback — mas "ficheiro não encontrado" é um erro de build (o ficheiro não foi incluído no bundle), enquanto "JSON inválido" é um erro de desenvolvimento (typo no JSON). O troubleshooting é diferente. (d) **Sem validação de schema.** O `AIConfig` é decoded com `JSONDecoder` — se um campo tem o tipo errado (ex: `temperature: "0.4"` em vez de `0.4`), a decode falha completamente, em vez de falhar só esse campo. (e) **Sem report de erro para o utilizador.** O erro é logado via `AppLog.provider.error` — mas o utilizador nunca vê esta mensagem. A app simplesmente comporta-se de forma estranha. (f) **Sem mecanismo de reparação.** Se o `ai_config.json` está corrompido, não há como o utilizador o substituir sem uma atualização da app. **Correção:** (1) Implementar validação de schema na carga: verificar que `providers` não está vazio, que cada provider tem `name`, `type`, e `defaultModel`, que `features` contém as chaves esperadas. (2) Em caso de falha, mostrar alerta modal no primeiro launch: "AI configuration is missing or corrupted. Some features will be unavailable. Please reinstall the app." (3) Separar erros: `fileNotFound` → erro crítico (build problem), `decodeFailed` → mostrar detalhes do erro de parsing (linha, campo). (4) Usar `JSONDecoder` com `DecodingError` handling granular: tentar decodificar cada provider individualmente, skip apenas os corruptos. (5) Adicionar `ai_config.json` hash verification no startup — comparar hash do ficheiro atual com o esperado (hardcoded) para detetar corrupção.

- [ ] **[#21] [P1] `AIProviderConfigModel` carregado do SwiftData não tem validação de integridade — `typeRaw` inválido faz fallback silencioso para `.openAICompatible`, `baseURLString` nil é aceite, `apiKeyKeychainIdentifier` não é verificado contra o Keychain.** `AIProviderConfigModel.swift:21` — `var type: ProviderType { ProviderType(rawValue: typeRaw) ?? .openAICompatible }`. **Problemas:** (a) **Fallback para `.openAICompatible` pode ser incorreto.** Se `typeRaw` é `"anthropic"` mas tem um typo (`"anthropc"`), o provider é tratado como OpenAI-compatible — o router usa endpoints OpenAI, o adapter injeta schema JSON nativo, e o provider real (Anthropic) não entende o formato. As chamadas API falham com erros enigmáticos. (b) **`baseURLString` nil não é validado.** Muitos providers requerem `baseURL` (especialmente `openAICompatible` e `localNetwork`). Se é nil, o router usa `"https://api.openai.com/v1"` como fallback (`ProviderRouter.swift:114`) — que pode não ser o endpoint correto. (c) **`apiKeyKeychainIdentifier` pode apontar para uma key que não existe.** Se o utilizador apagou a key do Keychain manualmente (via Settings > Passwords) ou a key expirou, o identifier continua guardado no SwiftData. O router tenta carregar a key, falha, e lança `ProviderError.missingAPIKey` — mas não atualiza o model para refletir que a key está missing. (d) **`availableModelsJSON` pode conter JSON inválido.** Se o JSON foi corrompido na gravação, o getter `availableModels` retorna `[]` silenciosamente — o utilizador vê "No models available" sem saber que é um erro de dados. (e) **Campos obrigatórios não são enforced.** `name` e `typeRaw` não têm validação `nil` ou empty — um provider sem nome aparece como "" na UI. (f) **Sem validação na deserialização do SwiftData.** Ao contrário de `init()` que pode ter defaults, quando o SwiftData popula o modelo a partir da base de dados, os valores são injetados diretamente — se um campo foi guardado com valor inválido, é carregado sem validação. **Correção:** (1) Adicionar método `validate() throws` ao `AIProviderConfigModel` que verifica: `name` não vazio, `typeRaw` é um `ProviderType` válido, `baseURLString` é URL válida ou nil, `apiKeyKeychainIdentifier` tem key correspondente no Keychain, `availableModelsJSON` é JSON válido ou nil. (2) Chamar `validate()` no `didSet` ou antes de usar o provider no router. (3) Se a validação falha, marcar o provider como `needsRepair: true` e mostrar banner no Settings. (4) Não fazer fallback silencioso para `.openAICompatible` — se `typeRaw` é inválido, lançar erro e mostrar "Unknown provider type: XYZ" na UI. (5) Verificar `apiKeyKeychainIdentifier` contra o Keychain periodicamente (no health check) e atualizar `keyStatus: present | missing | expired`.

- [ ] **[#22] [P1] `buildSettingsSnapshot()` em `ConfigProjectService` usa strings literais para chaves de UserDefaults em vez de partilhar constantes com `AutomationSettings`, `TranscriptionSettings`, `AudioSessionManager` — se uma chave é renomeada, o config project parte silenciosamente.** `ConfigProjectService.swift:144-189` — o método `buildSettingsSnapshot()` compila um dicionário com ~25 chaves. As chaves são strings hardcoded: `"autoTranscribe"`, `"autoAnalyze"`, `"transcriptionMode"`, `"voiceProcessing"`, etc. **Problemas:** (a) **Divergência de nomes.** `UserDefaults.standard.bool(forKey: "audio_raw_mode")` é lido como `"voiceProcessing": !audioRawMode` — o nome no JSON é o inverso do nome no UserDefaults. Se um developer muda `audio_raw_mode` para `audio_voice_processing`, o `buildSettingsSnapshot` continua a compilar (porque usa uma string literal diferente) mas o valor lido está errado. (b) **Chaves da App Group não incluídas.** `UserDefaults(suiteName: "group.com.wawa-note")` tem chaves como `pendingImportFiles` que não são expostas. (c) **Chaves do `ModelResolver` não incluídas.** `model_resolver_tiers` é um JSON serializado no UserDefaults — não aparece no snapshot. (d) **Chaves de feature flags não documentadas.** `has_shown_welcome`, `_onboarded_v1`, `_onboarded_v2` são lidas em vários sítios mas não são expostas nem documentadas. (e) **Sem tipagem.** O dicionário é `[String: Any]` — se um valor é `Bool`, `String`, ou `Int`, o JSON serializado perde a distinção (tudo é número ou string). (f) **Sem versão do schema.** Se o formato do settings JSON mudar, não há `version` field para o consumidor saber como interpretar. **Correção:** (1) Cada store (`AutomationSettings`, `TranscriptionSettings`, `AudioSessionManager`) deve expor um `var settingsSnapshot: [String: Any]` que o `buildSettingsSnapshot` agrega — eliminando a duplicação de chaves. (2) Usar `enum SettingKey: String, CaseIterable` com `key: String`, `type: SettingType`, `editable: Bool`, `description: String` para documentar cada chave. (3) Incluir chaves da App Group e feature flags com indicação de origem (`standard`, `appGroup`, `shared`). (4) Adicionar `version: 1` ao snapshot para permitir migração futura do formato. (5) Teste unitário que verifica que todas as chaves do `SettingKey` enum aparecem no snapshot e vice-versa.

- [ ] **[#23] [P2] Ficheiros JSON de overrides do utilizador (`prompts.json`, `skills.json`, `agent_memories.json`) não têm validação de schema ao gravar — JSON malformado grava silenciosamente e falha ao carregar no próximo launch, perdendo todas as edições do utilizador.** `PromptStore.saveOverrides()` (`ContentPipelineService.swift:1226-1243`) grava `prompts.json` com `JSONEncoder`. `AnalysisSkillStore.saveOverrides()` (`AnalysisSkillStore.swift:182-191`) grava `skills.json`. `AgentMemoryStore.save()` (`ContentPipelineService.swift:1351-1364`) grava `agent_memories.json`. **Problemas:** (a) **Gravação não atómica.** Se a app crasha durante `write(to:)`, o ficheiro fica parcialmente escrito (corrupto). Na próxima carga, `JSONDecoder` falha e o ficheiro é ignorado — todas as edições do utilizador são perdidas. (b) **Sem backup antes de sobrescrever.** Nenhum dos stores cria `.bak` ou `.old` antes de gravar. Se o novo ficheiro é inválido, não há como recuperar a versão anterior. (c) **`AgentMemoryStore.save()` usa `try?` e engole erros de encoding.** Se uma memória contém dados que não são `Encodable`, o `JSONEncoder` falha — mas o erro é silenciosamente ignorado. As memórias não são gravadas e o utilizador não sabe. (d) **Sem validação cruzada entre stores.** Se o utilizador edita `prompts.json` manualmente (fora da app, via Files) e introduz um erro de sintaxe, o `PromptStore` na próxima carga deteta o erro de decoding e... loga o erro, mantém os prompts built-in, e **remove** todos os overrides. As edições do utilizador são perdidas. (e) **Sem limite de tamanho.** `agent_memories.json` pode crescer indefinidamente (ver #21 da exploração) — eventualmente a gravação pode falhar por disco cheio, e o `try?` engole o erro. (f) **Conteúdo binário em memórias.** Se uma memória contém dados binários (ex: imagem codificada em base64), o JSON pode ficar enorme e lento para parse. **Correção:** (1) Gravação atómica: escrever para `prompts.json.tmp`, verificar que o ficheiro é válido (re-ler e decodificar), depois renomear para `prompts.json`. (2) Criar backup: antes de sobrescrever, copiar `prompts.json` para `prompts.json.bak`. Se a carga do ficheiro principal falhar, tentar o backup. (3) `AgentMemoryStore.save()` deve propagar erros de encoding — usar `try` em vez de `try?` e mostrar alerta se a gravação falhar. (4) Na carga, se o JSON é inválido, NÃO descartar os overrides — manter o ficheiro corrupto com nome `prompts.json.corrupt` e carregar os built-ins, mas notificar o utilizador: "Your prompt customizations could not be loaded. The file has been saved as prompts.json.corrupt for recovery." (5) Adicionar limite de tamanho ao `AgentMemoryStore`: máximo 500 memórias ou 5 MB, com evicção LRU. (6) Validar conteúdo antes de gravar: verificar que todos os objetos são `Encodable`, que não há referências cíclicas, e que o JSON resultante é ≤ 10 MB.

- [ ] **[#24] [P1] Sem integridade referencial entre items de configuração — `autoAnalysisModel` pode referenciar um modelo que não existe em nenhum provider, `activeProviderId` pode apontar para um provider que foi apagado, e `autoAnalysisProvider` pode ser um provider sem suporte a chat.** `AutomationSettings.shared` (`AutomationSettings.swift:13-55`) tem `autoAnalysisModel: String` e `autoAnalysisProvider: String?`. `ActiveProviderManager` (`ActiveProviderManager.swift:14-41`) tem `getActiveProviderID()`. **Problemas:** (a) **Modelo órfão.** Se o utilizador configura `autoAnalysisModel = "gpt-5-nano"` e depois remove o provider OpenAI, `autoAnalysisModel` continua a referenciar um modelo que nenhum provider oferece. A próxima análise falha com "model not found". (b) **Provider inativo como `autoAnalysisProvider`.** Se `autoAnalysisProvider = "openai"` e o utilizador muda o provider ativo para Anthropic, `autoAnalysisProvider` pode continuar a apontar para um provider que já não é usado. (c) **Provider sem capacidade necessária.** Se `autoAnalysisProvider` referencia um provider local que só suporta embeddings (`supportsChat: false`), a análise falha. (d) **`activeProviderId` stale.** Se o provider ativo é apagado, `ActiveProviderManager.getActiveProvider()` tenta carregar o UUID do SwiftData — se não existe, faz fallback para o primeiro provider disponível. Mas `getActiveProviderID()` (que só lê UserDefaults) continua a retornar o ID do provider apagado. (e) **Sem validação no setter.** `AutomationSettings.shared.autoAnalysisModel = "modelo-que-não-existe"` é aceite sem verificação. (f) **UI de seleção de modelo nos Settings (`SettingsView.swift:291-298`) valida no `onAppear`, mas outras views que leem `autoAnalysisModel` diretamente não validam.** **Correção:** (1) Adicionar `func validateReferences() -> [ConfigReferenceError]` que verifica: `autoAnalysisModel` existe em pelo menos um provider, `autoAnalysisProvider` é um provider válido, `activeProviderId` corresponde a um `AIProviderConfigModel` existente. (2) Chamar validação no setter de `autoAnalysisModel` e `autoAnalysisProvider` — rejeitar valores inválidos. (3) No `ProviderHealthMonitor`, se o provider ativo é apagado, limpar `activeProviderId` e forçar seleção de novo provider. (4) Adicionar `onDelete` hook no `AIProviderConfigModel`: quando um provider é apagado, verificar se era referenciado por `autoAnalysisProvider` ou `activeProviderId`, e limpar essas referências. (5) Expor erros de integridade no config project: item "Config Integrity" que lista todos os broken references com sugestões de reparação.

---

## N — Config Versioning & History

- [ ] **[#25] [P1] `ai_config.json` no bundle não tem versionamento — se a app é atualizada com novas definições de providers, features, ou lenses, não há migração dos overrides do utilizador para o novo schema, e alterações no bundle são aplicadas sem consideração pelos personalizações do utilizador.** `AIConfigService` (`AIConfigService.swift:71-108`) carrega `ai_config.json` do bundle na inicialização. `PromptStore` carrega built-in prompts do bundle, depois aplica overrides do `configs/prompts.json`. **Problemas:** (a) **Bundle update pode quebrar overrides.** Se a versão 2.0 do `ai_config.json` muda o nome de um prompt de `"analysis_system"` para `"analysis_v2_system"`, o override do utilizador em `prompts.json` para `"analysis_system"` é aplicado ao prompt antigo (que pode já não existir). O novo prompt `"analysis_v2_system"` não tem override. (b) **Novos prompts no bundle não são detetados.** Se a versão 2.0 adiciona `"contract_analysis"`, o `PromptStore` adiciona-o à lista de built-ins. Mas o utilizador não é notificado de que há novos prompts disponíveis. (c) **Lenses no bundle são imutáveis.** Se a versão 2.0 melhora uma lens existente, o utilizador não recebe a melhoria se tiver um override local. E não há como saber que a lens foi atualizada. (d) **Skills no bundle (`Resources/Skills/*.json`) são carregadas por nome de ficheiro.** Se uma skill é renomeada na nova versão, o `AnalysisSkillStore` vê-a como uma skill nova e mantém a antiga como órfã. (e) **Não há `configVersion` no `ai_config.json`.** Sem versão, é impossível saber se o ficheiro é da versão 1.0 ou 2.0, e que migrações são necessárias. **Correção:** (1) Adicionar campo `"version": 2` ao `ai_config.json` e `AIConfig`. (2) Implementar `AIConfigMigrationService` que, ao detetar `version` diferente da anterior, executa migrações: renomear prompts antigos → novos, merge de lenses, notificar skills novas. (3) `PromptStore` deve manter um registo de "prompts que o utilizador já viu" — novos prompts no bundle devem ser destacados como "New" no config project. (4) `AIConfigService` deve guardar a versão do `ai_config.json` da última execução em UserDefaults e, se mudou, emitir notificação `configBundleUpdated`. (5) Para cada atualização de bundle, mostrar changelog no Settings: "AI Config updated to v2: +2 prompts, ~1 lens, +1 skill".

- [ ] **[#26] [P2] `PromptStore` e `AnalysisSkillStore` não têm histórico de alterações — o utilizador não pode ver versões anteriores de um prompt, desfazer alterações, ou comparar o estado atual com o original.** `PromptStore` (`ContentPipelineService.swift:1141-1303`) guarda apenas o estado atual (built-in + overrides merged). `AnalysisSkillStore` (`AnalysisSkillStore.swift`) guarda `skills: [String: AnalysisSkill]` com flag `isUserEdited`. **Problemas:** (a) **Sem undo.** Se o utilizador edita um prompt, grava, e depois quer reverter — não pode. Tem de editar manualmente para o estado anterior (se souber o que era). (b) **Sem diff visual.** O utilizador não pode ver "o que mudou entre a versão base e a minha edição". O config project mostra o prompt final, sem indicação das partes editadas. (c) **Sem `resetToDefault` por prompt individual.** `PromptStore.resetAll()` repõe TODOS os prompts — não há `resetPrompt(named:)` para um único prompt. (d) **`AnalysisSkillStore.resetSkill` é broken para user-created skills** (ver #20 da exploração) — define `isUserEdited = false` e recarrega built-ins, mas se a skill não existe nos built-ins, desaparece. (e) **Sem changelog de "quem editou o quê e quando".** `isUserEdited` é um booleano — não diz quando foi editado, por quem (utilizador vs agente), ou quantas edições foram feitas. **Correção:** (1) Implementar `PromptHistoryStore` que guarda versões anteriores de cada prompt (`promptName`, `content`, `timestamp`, `editedBy`). Máximo 10 versões por prompt. (2) Adicionar `resetPrompt(named:)` ao `PromptStore` que remove o override do utilizador para um prompt específico. (3) Adicionar UI de histórico no KnowledgeDetailView para items do config project: "Version 3 (2 hours ago) · Version 2 (yesterday) · Version 1 (original)". (4) `AnalysisSkillStore.resetSkill` deve verificar se a skill existe nos built-ins; se não, perguntar "This skill was created by you. Reset will delete it. Continue?" (5) Adicionar metadados de edição: `lastEditedBy: "user" | "agent"`, `lastEditedAt: Date`, `editCount: Int`.

- [ ] **[#27] [P2] `AgentMemoryStore` não tem changelog ou audit trail — memórias são criadas, atualizadas, e removidas sem registo de quando, por quem, ou porquê. Se o agente corrompe uma memória, não há como saber o valor anterior.** `AgentMemoryStore` (`ContentPipelineService.swift:1306-1430`) tem `set(key:value:)` que faz upsert direto. **Problemas:** (a) **Sem registo de criação.** Quando uma memória é criada, não há entrada de log estruturada — apenas um `os_log` genérico se o caller o fizer. (b) **Sem registo de modificação.** Se o agente atualiza uma memória 5 vezes numa sessão, apenas o valor final é preservado. As versões intermédias são perdidas. (c) **Sem blame.** Não há como saber se uma memória foi criada pelo utilizador (via config project), pelo agente (via tool), ou pelo sistema (via pipeline de análise). (d) **Sem capacidade de audit para debugging.** Se o agente se comporta de forma estranha, não há como ver "que memórias foram modificadas nos últimos 10 minutos?". (e) **Memórias podem ser apagadas sem rasto.** `remove(key:)` apaga permanentemente — sem lixeira, sem confirmação. (f) **O config project expõe memórias como JSON array (`#5`), mas esse array é um snapshot — não reflete alterações em tempo real.** **Correção:** (1) Adicionar `AgentMemoryEntry` com `id`, `key`, `value`, `createdAt`, `updatedAt`, `createdBy`, `updatedBy`, `version`. Manter array de versões anteriores (máx 5). (2) `set()` incrementa `version` e guarda a entrada anterior no histórico. (3) `delete()` move para `deletedMemories` (soft delete) com `deletedAt`. `purgeDeleted()` limpa permanentemente após 30 dias. (4) Expor changelog no config project: item "Memory Changelog" com últimas 20 alterações em formato timeline. (5) Adicionar `AgentMemoryStore.recentChanges(since: Date) -> [MemoryChange]` para debugging.

- [ ] **[#28] [P3] Items do config project têm `updatedAt` mas não há vista de diff/histórico — o utilizador vê "Updated 2 hours ago" mas não sabe o que mudou, quem mudou, ou porquê.** Cada `KnowledgeItem` tem `updatedAt: Date` — mas é apenas um timestamp. **Problemas:** (a) **Sem diff entre versões.** Se o `syncConfigProject` atualiza um item (porque o provider mudou), o utilizador vê "Updated just now" mas não sabe que campo mudou. (b) **Sem indicação de "esta é uma alteração do sistema vs alteração tua".** Quando `syncConfigProject` recria items, o `updatedAt` é atualizado — mas o utilizador pode pensar que foi ele que editou. (c) **Sem resumo de alterações.** Se 15 items foram atualizados na última sync, o utilizador não recebe um sumário "3 providers updated, 2 prompts changed, 10 memories added". (d) **`updatedAt` é atualizado mesmo quando o conteúdo não muda.** `syncConfigProject` apaga e recria items com o mesmo conteúdo — o `updatedAt` muda na mesma. Falso positivo de alteração. (e) **Sem retenção de versões anteriores no SwiftData.** Apenas o estado atual do `KnowledgeItem` existe — não há `KnowledgeItemVersion` table. **Correção:** (1) `ConfigProjectService.syncConfigProject` deve comparar conteúdo antes de atualizar — se o JSON é idêntico, manter o `updatedAt` original. (2) Adicionar `changeDescription: String?` ao `KnowledgeItem` — `syncConfigProject` preenche com "Provider defaultModel changed: gpt-5-nano → gpt-5". (3) Expor `changeDescription` na UI do KnowledgeDetailView: "Changed 2 hours ago: defaultModel updated." (4) Guardar `lastSyncContentHash: String` no item — SHA256 do body. Comparar antes de atualizar. (5) Adicionar `ConfigChangelogView` acedível via menu "…" no config project: lista cronológica de todas as alterações com diff.

---

## O — Config Sync & Propagation

- [ ] **[#29] [P1] `active_provider_id` é escrito por duas fontes independentes — `ActiveProviderManager.setActiveProviderID` e `AnarlogConfigBridge.exportConfig` — ambas escrevem diretamente em `UserDefaults.standard` sem coordenação, criando potencial race condition e inconsistência.** `ActiveProviderManager.swift:32-41` — `getActiveProviderID()` lê de `UserDefaults.standard.string(forKey: "active_provider_id")`. O setter correspondente (`setActiveProviderID`) escreve na mesma chave. `AnarlogConfigBridge.swift:75` — `exportConfig` também escreve `userDefaults.set(activeProviderID, forKey: "active_provider_id")`. **Problemas:** (a) **Duas fontes de verdade.** `ActiveProviderManager` gere o provider ativo durante o uso normal. `AnarlogConfigBridge` escreve o provider ativo durante export/import de configuração anarlog. Se ambos correm em sequência rápida, o último write ganha. (b) **`AnarlogConfigBridge` escreve sem passar pelo `ActiveProviderManager`.** Isto significa que alterações ao provider ativo feitas via anarlog não passam pela lógica de validação do `ActiveProviderManager` (verificação de que o provider existe, fallback para first available, etc.). (c) **Sem notificação de alteração.** Quando `UserDefaults` é atualizado diretamente, observers como `ProviderRouter` ou `SettingsView` não são notificados — a UI pode mostrar o provider errado até ao próximo refresh. (d) **`UserDefaults` não é transacional.** Se a app crasha entre a escrita no UserDefaults e a atualização do SwiftData, o estado fica inconsistente: UserDefaults diz provider X, mas SwiftData não tem provider X como ativo (o campo `isActive` não existe em `AIProviderConfigModel`, a associação é puramente via UserDefaults). (e) **Chave string hardcoded em 2 ficheiros.** `"active_provider_id"` em `ActiveProviderManager.swift:17` e `AnarlogConfigBridge.swift:75` — se uma é alterada e a outra não, divergência silenciosa. **Correção:** (1) Centralizar TODAS as leituras/escritas de `active_provider_id` no `ActiveProviderManager` — expor método `setActiveProviderID(_:source:)` que valida, escreve em UserDefaults, e emite notificação. (2) `AnarlogConfigBridge` deve chamar `ActiveProviderManager.shared.setActiveProviderID` em vez de escrever diretamente. (3) Adicionar `static let activeProviderIDKey = "active_provider_id"` como constante partilhada. (4) Emitir `Notification.Name.activeProviderChanged` quando o valor muda, para que views e serviços possam reagir. (5) Adicionar validação: ao ler `active_provider_id`, verificar que o provider correspondente existe no SwiftData; se não, limpar e fazer fallback.

- [ ] **[#30] [P1] `AIConfigService` carrega `ai_config.json` UMA VEZ no `init()` e nunca mais — alterações ao ficheiro em runtime (ex: via config project, download de atualização, ou edição manual) NÃO são detetadas, e a configuração em memória fica stale até ao próximo launch.** `AIConfigService.swift:71-108` — o singleton carrega o config no `init()` e armazena-o na propriedade `let config: AIConfig`. **Problemas:** (a) **Sem hot-reload.** Se o utilizador edita um prompt, skill, ou lens via config project (quando o flow inverso for implementado), o `AIConfigService` em memória não reflete a alteração. A próxima chamada ao agente usa a versão antiga. (b) **Sem mecanismo de invalidação.** Não há `AIConfigService.reload()` público que possa ser chamado após alterações de configuração. (c) **Os stores secundários (`PromptStore`, `AnalysisSkillStore`, `AgentMemoryStore`) são atualizados em memória, mas o `AIConfigService` não.** O `PromptStore.addOrUpdate()` atualiza o dicionário em memória e grava no disco — mas `AIConfigService.shared.config.features` não é atualizado. (d) **Novos providers não são registados no `AIConfigService`.** Se o utilizador adiciona um provider customizado (ex: Azure OpenAI), o `AIProviderConfigModel` é criado no SwiftData, mas `AIConfigService.shared.config.providers` não inclui o novo provider — o `ProviderRouter` usa o `AIConfigService` para resolver endpoints e o provider customizado não tem endpoint mapping. (e) **Download de modelo local não atualiza a config.** Se o utilizador faz download de um modelo llama.cpp, o `ModelRegistry` regista-o — mas o `AIConfigService` não sabe que há um novo modelo local disponível. **Correção:** (1) Adicionar `AIConfigService.reload()` que recarrega `ai_config.json` do bundle e re-aplica overrides. (2) Emitir notificação `AIConfigService.configDidChange` sempre que `reload()` é chamado. (3) `PromptStore.addOrUpdate()` e `AnalysisSkillStore.addOrUpdate()` devem chamar `AIConfigService.reload()` (ou invalidar cache específica). (4) `AIProviderConfigModel.onDidSave` deve notificar `AIConfigService` para regenerar o mapeamento de providers. (5) Implementar `AIConfigService.refreshProviders()` que sincroniza `config.providers` com `AIProviderConfigModel` (SwiftData) — merge entre bundle templates e user-configured providers.

- [ ] **[#31] [P2] `AutomationSettings.autoAnalysisModel` e `autoAnalysisProvider` não são revalidados automaticamente quando o provider ativo muda ou quando um provider é removido — apenas o `SettingsView.onAppear` faz validação, deixando outras views e serviços com referências stale.** `SettingsView.swift:291-298` — o `onAppear` verifica se `autoAnalysisModel` existe em `availableAutoAnalysisModels` e, se não, faz reset. `AutomationSettings.shared` (`AutomationSettings.swift:13-55`) não tem validação própria. **Problemas:** (a) **Views que leem `autoAnalysisModel` diretamente podem usar valor inválido.** Se `CaptureViewModel` lê `AutomationSettings.shared.autoAnalysisModel` para mostrar "Will analyze with: gpt-5-nano", e o utilizador removeu o provider OpenAI — o label mostra um modelo que não existe. (b) **Pipeline de análise pode falhar silenciosamente.** `ContentPipelineService` usa `autoAnalysisModel` e `autoAnalysisProvider` para selecionar o modelo de análise. Se o modelo não existe, o `ProviderRouter` vai falhar com erro — mas o erro pode ser interpretado como "análise falhou" em vez de "configuração inválida". (c) **`autoAnalysisProvider` pode ser nil.** Se é nil, o sistema tenta usar o provider ativo — que pode não suportar o modelo `autoAnalysisModel`. (d) **Validação só acontece quando o utilizador ABRE o SettingsView.** Se o utilizador nunca abre o Settings depois de remover um provider, a configuração inválida persiste. (e) **Sem validação proativa no `ProviderConnectView` ou `ProviderEditorView`.** Quando um provider é removido, não há verificação de "este provider era usado para auto-análise — deseja escolher outro?". **Correção:** (1) Mover validação para `AutomationSettings.shared` — `didSet` de `autoAnalysisModel` e `autoAnalysisProvider` deve validar contra providers existentes. (2) Adicionar `AutomationSettings.validateAutomationConfig()` que é chamada: no launch, quando um provider é adicionado/removido, e quando o provider ativo muda. (3) `ProviderEditorView.onDelete` deve verificar se o provider era referenciado e pedir confirmação. (4) `ContentPipelineService` deve validar a configuração ANTES de iniciar a análise e reportar erro específico: "Automation misconfigured: model 'gpt-5-nano' not available in any provider. Check Settings." (5) Adicionar indicador visual no Settings: se a automação está mal configurada, mostrar ⚠️ no tab do Settings.

- [ ] **[#32] [P1] Três stores de modelos separadas sem garantias de consistência — `AIProviderConfigModel` (SwiftData) tem providers e modelos, `AutomationSettings` (UserDefaults) tem `autoAnalysisModel`, `ActiveProviderManager` (memória + UserDefaults) tem `activeProviderId`. Nenhuma impõe consistência entre elas.** O sistema tem 3 fontes de verdade sobre "que modelos estão disponíveis e qual usar": (1) `AIProviderConfigModel.availableModels` — os modelos de cada provider (persistidos em SwiftData), (2) `AutomationSettings.autoAnalysisModel` — o modelo padrão para análise automática (UserDefaults), (3) `ActiveProviderManager` — o provider ativo + routing rules (UserDefaults + SwiftData). **Problemas:** (a) **Adicionar um provider não atualiza `autoAnalysisProvider`.** Se o utilizador adiciona o primeiro provider, `autoAnalysisProvider` continua nil — a automação não sabe que provider usar. (b) **Remover um provider não limpa `autoAnalysisModel`.** O modelo órfão persiste em UserDefaults. (c) **`availableModels` pode estar desatualizado.** O campo `availableModelsJSON` no `AIProviderConfigModel` é populado apenas quando o utilizador faz "Connect" ou "Fetch Models" — se o provider adiciona novos modelos (ex: OpenAI lança `gpt-5.6`), o utilizador não sabe. (d) **Model aliases não são resolvidos.** Se o utilizador define `autoAnalysisModel = "gpt-5-nano"` e depois muda o `defaultModel` do provider para `"gpt-5"`, a automação continua a usar `"gpt-5-nano"` (que ainda pode existir, mas não é o default). (e) **Sem "modelo atual" unificado.** `AgentLoop` usa `executorModel` e `advisorModel` passados pelo caller. `ContentPipelineService` usa `autoAnalysisModel`. `ChatViewModel` usa `ActiveProviderManager` → `defaultModel`. Não há "qual é o modelo que devo usar para X?" unificado. (f) **Fallback tiers do `ModelResolver` não são usados.** `ModelResolver` existe (`Anarlog/ModelResolver.swift`) com tiers de fallback sofisticados — mas NÃO é usado pelo `AgentLoop`, `ContentPipelineService`, ou `ChatViewModel`. É apenas visível no Settings UI. **Correção:** (1) Implementar `ModelResolutionService` (unificado) que substitui a lógica dispersa: `resolveModel(for: Feature, preferredModel: String?, preferredProvider: String?) -> (provider: AIProviderConfigModel, model: String)`. (2) `ModelResolutionService` usa `ModelResolver.tiers` como fallback. (3) Quando um provider é adicionado/removido, `ModelResolutionService` invalida caches e revalida configurações pendentes. (4) `AIProviderConfigModel.availableModels` deve ter TTL (ex: 24h) e ser re-fetchable em background. (5) Expor estado de consistência no config project: item "Model Consistency" que mostra "✅ autoAnalysisModel 'gpt-5-nano' available in provider 'openai'" ou "❌ autoAnalysisModel 'gpt-5-nano' not found in any provider".

- [ ] **[#33] [P2] Watch Connectivity transmite estado de gravação mas NÃO transmite configuração — o Watch não tem acesso a providers, prompts, ou settings, limitando a sua capacidade de operar independentemente.** `iOSWatchSessionManager` (`Connectivity/iOSWatchSessionManager.swift`) envia `RecordingStatus` (state, elapsedTime, audioLevel, title, error). **Problemas:** (a) **Watch não pode iniciar análise.** Se o utilizador grava uma reunião pelo Watch, a gravação é enviada para o iPhone — mas o Watch não sabe que provider usar para transcrição/ análise. (b) **Watch não pode mostrar estado de configuração.** O utilizador não sabe, no Watch, se há um provider configurado, se a API key é válida, ou se a auto-análise está ligada. (c) **Watch não pode gravar offline.** Se o iPhone está fora de alcance, o Watch não sabe se há capacidade de transcrição local. (d) **Config do Watch é um subconjunto do iPhone mas não há sync.** As definições de áudio (voice processing, speakerphone) poderiam ser úteis no Watch, mas não são transmitidas. (e) **Watch app pode ter o seu próprio `ai_config.json`?** Não está claro se a Watch app incluída no target tem acesso ao bundle de configuração ou se depende totalmente do iPhone. **Correção:** (1) Transmitir `WatchConfigSnapshot` periodicamente: `activeProviderName`, `autoTranscribeEnabled`, `autoAnalyzeEnabled`, `availableModelsCount`, `configErrors: [String]`. (2) Watch usa `WatchConfigSnapshot` para mostrar estado no ecrã principal: "✅ OpenAI · gpt-5-nano" ou "⚠️ No provider configured". (3) Watch envia comandos de configuração básicos: "Toggle Auto-Transcribe" sem precisar do iPhone. (4) Watch app bundle deve incluir um `ai_config.json` mínimo (apenas metadados, sem API keys) para referência offline.

---

## P — Security of Configurable Elements

- [ ] **[#34] [P1] `SecureKeyStore` usa `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly` — as API keys ficam acessíveis enquanto o dispositivo está ligado (mesmo bloqueado), permitindo que pipelines em background usem as keys, mas também expondo-as a qualquer processo que consiga aceder ao Keychain com o dispositivo bloqueado.** `SecureKeyStore.swift:32-35` — o comentário explica que `AfterFirstUnlock` foi escolhido porque "pipelines need access while device is locked." **Problemas:** (a) **Janela de exposição longa.** Após o primeiro unlock (que acontece no boot), as keys ficam acessíveis até o dispositivo ser reiniciado — potencialmente dias ou semanas. Durante este tempo, qualquer app com acesso ao Keychain (mesma team ID) pode ler as keys se o utilizador desbloqueou o dispositivo uma vez. (b) **Alternativa mais segura existe.** `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` limita o acesso a quando o dispositivo está desbloqueado — mas bloquearia pipelines em background. (c) **Não há distinção entre keys.** Todas as API keys usam o mesmo acesso. Keys para transcrição (que precisam de correr em background) têm o mesmo nível de proteção que keys para análise (que podem esperar pelo unlock). (d) **Sem `kSecAccessControl` com biometria.** Não há opção de exigir Face ID / Touch ID para aceder a API keys específicas — o utilizador não pode dizer "a key da OpenAI precisa de Face ID, a key local não". (e) **O atributo `ThisDeviceOnly` é bom** — impede que as keys sejam sincronizadas via iCloud Keychain para outros dispositivos. Mas não impede acesso local. **Correção:** (1) Adicionar enum `KeychainAccessLevel`: `whenUnlocked` (para keys que não precisam de background), `afterFirstUnlock` (para keys que precisam de background). (2) `SecureKeyStore.saveAPIKey` deve aceitar `accessLevel: KeychainAccessLevel` e aplicar o atributo correspondente. (3) Keys de transcrição e análise automática (que correm em background) usam `afterFirstUnlock`; keys de chat e export (que só correm com a app ativa) usam `whenUnlocked`. (4) Adicionar opção "Require Face ID for API keys" nos Settings — se ativa, adiciona `kSecAccessControlBiometryCurrentSet` ao `SecAccessControlCreateFlags`. (5) Auditar todos os callers de `SecureKeyStore` para categorizar o nível de acesso necessário.

- [ ] **[#35] [P1] `sanitizedForLog` é opt-in — os callers têm de se lembrar de chamar `.sanitizedForLog` nas strings antes de as passar ao logger. Se um developer se esquece, API keys e headers sensíveis são escritos em texto plano nos ficheiros de log.** `Logging.swift:237-259` — a extensão `String.sanitizedForLog` aplica regex para redactar patterns conhecidos. Mas a aplicação depende de cada caller invocar `.sanitizedForLog` explicitamente. **Problemas:** (a) **Fácil de esquecer.** Um developer que escreve `AppLog.provider.debug("Request body: \(body)")` — se `body` contém um header `Authorization: Bearer sk-...`, é escrito em texto plano. (b) **URLs com API keys.** Se um URL contém `?api_key=sk-...` (comum em alguns providers), o `sanitizedForLog` NÃO redacta porque o pattern `Bearer` não está presente no URL. (c) **Headers dictionary.** Se o código loga `headers.description`, o dicionário inteiro é escrito — `sanitizedForLog` pode não cobrir todos os formatos. (d) **OSLog vs FileLog.** O `sanitizedForLog` é aplicado ao mesmo string — mas se o caller faz `os_log("\(body)")` diretamente (bypassando o `AppLog` helper), o redaction não acontece. (e) **Regex compilation com `try?`.** Se a regex é inválida (`Logging.swift:253-255`), o `sanitizedForLog` retorna a string original sem redaction — exatamente o oposto do comportamento seguro. (f) **Logs antigos em disco.** `wawa-debug.log` mantém até 3 rotações de 1 MB cada. Se uma API key foi logada acidentalmente numa sessão anterior, permanece em disco até ser rodada. **Correção:** (1) `sanitizedForLog` deve ser aplicado automaticamente pelo `AppLog` helpers — os métodos `debug()`, `error()`, `warn()`, `event()` devem chamar `.sanitizedForLog` internamente antes de escrever. (2) Adicionar `sanitizedForLog` para URLs com query parameters sensíveis (`api_key`, `key`, `token`, `secret`). (3) Implementar `SafeLogString` wrapper que aplica sanitização no `init` e é usado como tipo nos parâmetros de log. (4) `FileLogService.retrieveLogs()` deve aplicar sanitização ao output — segunda linha de defesa. (5) Adicionar `logRedactionAudit()` que escaneia ficheiros de log existentes em busca de patterns de API key e reporta "⚠️ 3 potential API key leaks found in logs. [Review] [Purge]".

- [ ] **[#36] [P2] `FileArtifactStore` tem fallback para `cachesDirectory` quando o `applicationSupportDirectory` não está disponível — ficheiros em caches NÃO têm proteção de encriptação e podem ser purgados pelo sistema, expondo dados de configuração.** `FileArtifactStore.swift:33-39` — o `init` tenta criar o diretório base em `applicationSupportDirectory`. Se falha, faz fallback para `cachesDirectory`. `FileArtifactStore.swift:47-52` — `applyBaseProtection()` aplica `FileProtectionType.completeUnlessOpen` ao diretório base. Mas este método só é chamado APÓS a criação do diretório — e os `try?` nas linhas 48-52 engolem falhas. **Problemas:** (a) **Caches não são encriptados.** `cachesDirectory` não tem proteção de encriptação automática — ficheiros de configuração (`configs/prompts.json`, `configs/skills.json`, `configs/agent_memories.json`) podem conter informação sensível derivada de reuniões. (b) **Caches podem ser purgados pelo sistema.** iOS pode limpar `cachesDirectory` quando o espaço em disco está baixo — os ficheiros de configuração do utilizador desaparecem sem aviso. (c) **`try?` engole falhas de proteção.** Se `setResourceValues` falha (ex: permissões, disco cheio), o diretório fica sem encriptação. A app continua como se nada tivesse acontecido. (d) **Ficheiros individuais não têm proteção explícita.** `applyBaseProtection` aplica proteção ao diretório — ficheiros criados depois herdam a proteção. Mas se um ficheiro foi criado ANTES da proteção ser aplicada, fica desprotegido. (e) **`configsDirectoryURL()` retorna um path sob `baseURL`.** Se `baseURL` está em `cachesDirectory`, os ficheiros de config estão em caches — o utilizador não sabe. **Correção:** (1) `FileArtifactStore` deve recusar-se a usar `cachesDirectory` para dados persistentes — se `applicationSupportDirectory` não está disponível, mostrar erro ao utilizador e não criar ficheiros. (2) `applyBaseProtection` deve lançar erro em vez de usar `try?` — se a encriptação falha, a app deve alertar o utilizador. (3) Verificar proteção ao iniciar: `FileArtifactStore.verifyProtection()` que lê `FileProtectionType` do diretório base e emite warning se não é `completeUnlessOpen`. (4) Para ficheiros de config especificamente, aplicar `FileProtectionType.complete` (só acessível quando desbloqueado) — mais restritivo que `completeUnlessOpen`. (5) Adicionar `isProtected: Bool` ao item "App Settings" no config project para que o utilizador saiba o estado de segurança.

- [ ] **[#37] [P2] `DebugLogView` no Settings (`SettingsView.swift:305-383`) expõe TODOS os logs da app, incluindo OSLog e file log — se o utilizador partilha logs para debugging, pode inadvertidamente expor API keys, transcrições, ou dados de reuniões que não foram redactados.** `SettingsView.swift:305-383` — `DebugLogView` mostra logs de `FileLogService` e `OSLog` com search e filtro. **Problemas:** (a) **Logs podem conter dados sensíveis não redactados.** Ver #35 — se um developer esqueceu `sanitizedForLog`, o log contém API keys em texto plano. Quando o utilizador abre o DebugLogView, esses dados são visíveis. (b) **Partilha de logs é comum em debugging.** O utilizador pode fazer screenshot ou partilhar o ficheiro de log — expondo involuntariamente dados sensíveis a terceiros. (c) **Sem aviso de segurança ao exportar.** O botão "Export Logs" (`FileLogService.exportLogs()`) cria um ficheiro temporário e abre a share sheet — sem aviso "This log file may contain personal data. Review before sharing." (d) **Filtro de categorias não inclui "provider".** `retrieveOSLogs()` (linha 369-383) filtra por "audio", "recording", "route", "engine", "session", "segment" — mas não filtra "provider" ou "api" ou "key". Ironicamente, os logs de provider (que podem conter dados sensíveis) NÃO aparecem no DebugLogView — o que é bom para segurança, mas significa que problemas de provider não podem ser debugados via UI. (e) **Log retention é 3 × 1 MB = 3 MB.** Se um log antigo contém uma API key, permanece em disco por semanas (até ser rodado 3 vezes). **Correção:** (1) Adicionar banner de segurança no topo do DebugLogView: "⚠️ Logs may contain personal data. Review before sharing. API keys are automatically redacted." (2) `FileLogService.exportLogs()` deve aplicar sanitização adicional antes de exportar (segunda passagem de redaction). (3) Adicionar opção "Export Sanitized Logs" que aplica redaction agressiva (substitui TODOS os potenciais dados sensíveis, incluindo conteúdo de mensagens). (4) Expandir filtro de categorias no DebugLogView para incluir "provider" e "agent" com toggle. (5) Adicionar `LogPurgeService` que remove ficheiros de log com mais de 7 dias ou após partilha bem-sucedida.

- [ ] **[#38] [P3] `BiometricGateService.isEnabled` persiste o estado da gate biométrica no Keychain como um valor literal `"1"` ou `"0"` — usa a mesma proteção `AfterFirstUnlock` do `SecureKeyStore`, o que é overkill para um booleano de configuração mas sub-protege o mecanismo de gate.** `WawaNoteApp.swift:189-234` — `BiometricGateService` usa `SecureKeyStore` com identifier `com.wawa-note.biometric-gate`. `SecureKeyStore.saveAPIKey("1")` / `loadAPIKey` / `deleteAPIKey`. **Problemas:** (a) **Usar `saveAPIKey` para um booleano é semanticamente errado.** O método chama-se `saveAPIKey` — mas o valor guardado é `"1"` (gate enabled) ou `"0"` (gate disabled). Não é uma API key. (b) **A proteção `AfterFirstUnlock` é inadequada para uma gate de segurança.** A gate biométrica controla o acesso à app. Mas o estado da gate (enabled/disabled) pode ser lido por qualquer processo com acesso ao Keychain após o primeiro unlock — um attacker que queira desligar a gate pode, teoricamente, modificar este valor. (c) **Sem binding biométrico para modificar o estado.** Idealmente, DESLIGAR a gate biométrica deveria exigir autenticação biométrica — mas `BiometricGateService.isEnabled = false` é um setter normal, sem autenticação. (d) **O valor `"1"` não tem integridade criptográfica.** Um processo malicioso pode escrever `"0"` no Keychain com o mesmo identifier e desligar a gate. (e) **`SecureKeyStore` não foi desenhado para dados de configuração.** Usa `kSecClassGenericPassword` — semanticamente, devia ser `kSecClassKey` ou um wrapper dedicado. **Correção:** (1) Criar `SecurePreferenceStore` separado para dados de configuração sensíveis (gate state, flags de segurança) com `kSecClassGenericPassword` mas sem o nome "API key". (2) Aplicar `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` para a gate state (só acessível quando desbloqueado). (3) Adicionar `kSecAccessControlBiometryCurrentSet` ao estado da gate — só pode ser modificado com autenticação biométrica. (4) Adicionar hash HMAC do valor para detetar tampering. (5) Separar conceitos: `SecureKeyStore` para API keys, `SecurePreferenceStore` para preferências de segurança.

---

## Q — User Journeys (Config)

- [ ] **[#39] [P1] Primeira experiência de configuração (first launch) não tem guided setup — o utilizador abre a app, o config project é criado vazio (sem providers), e não há wizard ou onboarding que guie a configuração inicial.** `ContentView.swift:128-129` — `ConfigProjectService.ensureConfigProject` e `syncConfigProject` correm no primeiro launch. Se não há providers configurados, o config project tem apenas "App Settings" (25 keys de UserDefaults) e talvez prompts built-in — mas sem providers, prompts não podem ser usados. **Problemas:** (a) **App funciona mas parece broken.** Sem providers, o Chat mostra "No provider configured". O Capture mostra "Auto-analysis requires a provider". O utilizador não sabe o que fazer. (b) **Sem wizard de boas-vindas.** Não há ecrã "Welcome to Wawa Note! Let's set up your first AI provider." com opções: "Connect OpenAI", "Connect Anthropic", "Use Local Model", "Skip for now". (c) **Primeiro launch não verifica estado mínimo de configuração.** A app podia verificar: 0 providers → sugerir adicionar um. 1+ providers mas nenhum ativo → sugerir ativar um. Provider ativo sem API key → sugerir adicionar key. (d) **Settings UI não destaca ações prioritárias.** O Settings mostra todas as opções igualmente — não há "Getting Started" checklist: [ ] Add AI Provider, [ ] Configure Auto-Transcription, [ ] Set Analysis Preferences. (e) **Sem tooltips ou ajuda contextual.** O utilizador vê "Provider" mas não sabe o que é um provider, que tipos existem, ou qual escolher. (f) **Primeira gravação sem config.** Se o utilizador grava uma reunião sem ter configurado transcrição, a gravação é guardada mas não processada — sem aviso de que falta configuração. **Correção:** (1) Implementar `OnboardingConfigView` — wizard de 3 passos: (a) "Choose AI Provider" — lista de templates com descrições, (b) "Add API Key" — campo seguro com validação, (c) "Configure Automation" — toggles para auto-transcrição e auto-análise. (2) `ContentView.onAppear` deve verificar `hasCompletedOnboarding` flag — se false, mostrar onboarding. (3) Settings deve ter secção "Getting Started" no topo (colapsável após conclusão) com checklist visual: ✅ Add Provider, ✅ Configure Key, ⬜ Set Preferences. (4) Antes da primeira gravação, se não há provider de transcrição, mostrar alerta: "Transcription requires an AI provider with audio support. Set up now? [Setup] [Record Anyway]". (5) Tooltips nos campos do Settings: ícone ⓘ ao lado de cada setting que mostra descrição.

- [ ] **[#40] [P1] O fluxo de conexão de provider (`ProviderConnectView`) grava a API key no Keychain ANTES de testar a conexão — se o teste falha, a key é apagada, mas se o `deleteAPIKey` falha, a key fica órfã no Keychain.** `ProviderConnectView.swift:122-138, 174-184, 221-223` — o fluxo: (1) utilizador introduz API key, (2) `keychain.saveAPIKey(key, for: tempKeyId)` — grava imediatamente, (3) testa conexão com o provider, (4) se sucesso: cria `AIProviderConfigModel` com `tempKeyId`, (5) se falha: `keychain.deleteAPIKey(for: tempKeyId)`. **Problemas:** (a) **Keychain pode reter keys órfãs.** Se `deleteAPIKey` falha (raro, mas possível — Keychain pode estar corrompido ou sem acesso), a key fica no Keychain com um identifier aleatório que não está associado a nenhum `AIProviderConfigModel`. (b) **Keychain poluição ao longo do tempo.** Cada tentativa de conexão falhada cria um novo `tempKeyId = UUID().uuidString` e escreve no Keychain. Após 20 tentativas falhadas, há 20 entradas órfãs no Keychain. (c) **Sem limpeza de keys órfãs.** Não há rotina de manutenção que percorre o Keychain e remove entradas não referenciadas por nenhum `AIProviderConfigModel`. (d) **Teste de conexão usa a key real.** Se o utilizador escreveu a key errada, a key errada é enviada para o servidor do provider — que pode registar tentativas falhadas de autenticação. (e) **Sem throttling de tentativas de conexão.** O utilizador pode fazer 50 tentativas de conexão em rápida sucessão — cada uma escreve e apaga do Keychain, e faz um pedido HTTP ao provider. **Correção:** (1) Só gravar a API key no Keychain APÓS o teste de conexão ser bem-sucedido: guardar a key em memória (`private var pendingKey: String?`) durante o teste, e só chamar `keychain.saveAPIKey` depois de confirmar que a conexão funciona. (2) Adicionar `KeychainMaintenanceService.cleanupOrphanedKeys()` que: (a) lista todos os `AIProviderConfigModel.apiKeyKeychainIdentifier`, (b) lista todas as entradas no Keychain com service `com.wawa-note.keychain`, (c) remove as não referenciadas. (3) Chamar `cleanupOrphanedKeys()` no launch e após cada conexão de provider. (4) Adicionar throttling: máximo 5 tentativas de conexão por minuto por provider type. (5) Mostrar "Testing connection..." com spinner e desabilitar o botão durante o teste para evitar double-tap.

- [ ] **[#41] [P2] O config project pode aparecer em listas de projetos se o filtro `isConfigProject` não for aplicado — views que fazem queries diretas ao SwiftData sem usar `nonConfigProjects` expõem "Wawa Note Config" como um projeto normal.** `ConfigProjectService.isConfigProject(project:)` verifica `slug == "wawa-note-config"`. `ProjectService.nonConfigProjects` aplica este filtro. **Problemas:** (a) **Dependência de os developers usarem `nonConfigProjects`.** Se uma nova view faz `FetchDescriptor<Project>` sem predicate, o config project aparece. (b) **Agent tools podem não aplicar o filtro.** `ListProjectsTool` pode listar todos os projetos sem excluir o config project. (c) **UI de seleção de projeto (ex: mover item para projeto) pode mostrar o config project como destino.** O utilizador pode acidentalmente mover uma nota para o config project — onde será apagada na próxima sync. (d) **Share Extension pode ver o config project.** Se a Share Extension lista projetos para o utilizador escolher onde importar um ficheiro, o config project pode aparecer na lista. (e) **Sem ícone ou badge visual.** Se o config project aparece, parece um projeto normal — mesmo nome, mesma cor. O utilizador não tem como saber que é especial. **Correção:** (1) Em vez de depender de os callers filtrarem, adicionar propriedade `isHidden: Bool` ao modelo `Project` — o config project é criado com `isHidden = true`. (2) `FetchDescriptor` padrão em `ProjectService` deve sempre incluir `#Predicate { $0.isHidden == false }` — com opção `includeHidden: true` para casos especiais. (3) `ListProjectsTool` deve respeitar `isHidden`. (4) Dar ao config project um ícone distinto (⚙️) e cor diferente (cinza) para identificação visual. (5) Se o utilizador tenta adicionar um item ao config project, mostrar alerta: "This is a system configuration project. Adding items here may cause them to be deleted. Continue? [Cancel] [Add Anyway]".

- [ ] **[#42] [P2] Não há operação "Reset to Defaults" para itens de configuração individuais — o utilizador não pode repor um prompt, skill, ou setting específico para o valor original sem apagar e reconfigurar manualmente.** `PromptStore` tem `resetAll()` mas não `resetPrompt(named:)`. `AnalysisSkillStore` tem `resetSkill(named:)` — mas é broken para skills criadas pelo utilizador (ver #20). `AutomationSettings` não tem `resetAll()`. `AgentMemoryStore` não tem `resetAll()` nem `reset(key:)` para o valor original (não há "original" para memórias). **Problemas:** (a) **Reset de prompt individual impossível.** Se o utilizador editou "analysis_system" e quer voltar ao original — tem de se lembrar do texto original ou apagar a app e reinstalar. (b) **Reset de settings individuais não é exposto.** `AutomationSettings.shared.autoTranscribe = true` — mas qual era o default? O utilizador não sabe. (c) **Reset de skill é broken.** `AnalysisSkillStore.resetSkill(named:)` define `isUserEdited = false` e recarrega built-ins — mas se a skill não existe nos built-ins, desaparece. (d) **Sem "Reset All Configuration".** O utilizador não pode fazer reset completo da configuração para defaults de fábrica mantendo os dados (reuniões, projetos). (e) **Sem preview do reset.** "Se eu fizer reset deste prompt, qual era o texto original?" — sem preview, o utilizador não sabe se quer mesmo fazer reset. **Correção:** (1) `PromptStore`: adicionar `resetPrompt(named:)` que remove o override do `prompts.json` e restaura o built-in. (2) `AutomationSettings`: adicionar `static let defaults` com todos os valores padrão, e `resetAll()` / `reset(key:)` que restaura o default. (3) `AnalysisSkillStore.resetSkill`: verificar se a skill existe nos built-ins; se sim, restaurar; se não, perguntar "Delete this custom skill?". (4) Adicionar "Reset to Default" em cada item do config project — botão no KnowledgeDetailView que mostra preview do valor original antes de confirmar. (5) Settings → "Reset All Configuration" com confirmação dupla e lista do que será afetado: "This will reset: 5 prompts, 7 skills, 25 settings. Your meetings and projects will NOT be affected."

---

## R — Bugs & Defects

- [ ] **[#43] [P2] `ConfigProjectService.createConfigProject()` tem um `return project` duplicado — linha 60 retorna, linha 61 é código morto inalcançável.** `ConfigProjectService.swift:60-61`:
```swift
return project
return project
```
**Problemas:** (a) **Código morto.** O segundo `return` nunca é executado — a função retorna na linha 60. (b) **Indica possível refactor incompleto.** Duas linhas de return sugerem que havia lógica condicional que foi removida, ou que o developer pretendia adicionar algo entre os returns. (c) **Warning de compilador.** O Swift compiler emite um warning "Code after 'return' will never be executed" — polui o output de build. (d) **Baixo risco, mas sinal de falta de revisão.** Se um bug destes passou, outros podem ter passado. **Correção:** (1) Remover a linha 61 (`return project` duplicado). (2) Adicionar regra de linting (SwiftLint `unreachable_code`) para detetar automaticamente.

- [ ] **[#44] [P2] `workspace_config.json` e `navigation_config.json` estão no bundle (`Resources/`) mas NUNCA são lidos por nenhum código — dois ficheiros de configuração completos que são dead code.** Exploração confirmou: `grep -r "workspace_config"` e `grep -r "navigation_config"` em todos os ficheiros Swift retornam zero resultados. **Problemas:** (a) **Configuração definida mas não usada.** `workspace_config.json` (49 linhas) define `itemTypes`, `contextSensors`, `smartFolders`, `defaultViews` — mas estes valores são hardcoded nos enums (`KnowledgeItemType`, sensores em `ContextCaptureService`) e não são lidos do JSON. (b) **`navigation_config.json` (55 linhas) define `sidebar`, `smartFilters`, `detailSections` — mas a navegação é gerida pelo `ContentView` com tabs fixos (Capture, Inbox, Explore, Chat), não por configuração. (c) **Custo de manutenção.** Se alguém atualiza `navigation_config.json` pensando que a UI vai mudar, a alteração não tem efeito — frustração e desperdício de tempo. (d) **Falsa sensação de flexibilidade.** A existência destes ficheiros sugere que a app é data-driven — mas na realidade é hardcoded. (e) **Espaço no bundle.** São ~104 linhas de JSON — insignificante para espaço, mas significativo para confusão de arquitetura. **Correção:** (1) **Opção A (usar os ficheiros):** Implementar leitura de `workspace_config.json` no `ContentPipelineService` e `ContextCaptureService` para que os sensores e item types sejam configuráveis. Implementar leitura de `navigation_config.json` no `ContentView` para que sidebar e filtros sejam dinâmicos. (2) **Opção B (remover os ficheiros):** Se a arquitetura é hardcoded por design, remover `workspace_config.json` e `navigation_config.json` do bundle para evitar confusão. Mover as definições para código (enums). (3) **Recomendação:** Opção A — expor estes ficheiros no config project como items `"Workspace Config"` e `"Navigation Config"`, permitindo edição avançada.

- [ ] **[#45] [P2] `AgentMemoryStore.search()` aceita parâmetros `minDuration` e `minChars` mas NÃO os usa no filtro — os parâmetros são declarados na função mas ignorados na closure de filtragem.** `ContentPipelineService.swift:1403-1414` — `AgentMemoryStore.search(itemType:language:minDuration:minChars:contentType:maxResults:)`. O filtro interno (linha ~1408) apenas verifica `itemType` e `language`. **Problemas:** (a) **Parâmetros fantasmas.** `minDuration` e `minChars` são aceites mas ignorados — um caller que passa `minChars: 1000` espera não receber memórias curtas, mas recebe-as na mesma. (b) **`AgentMemory` tem campos `duration` e `charCount`?** Se existem no modelo, o filtro devia usá-los. Se não existem, os parâmetros não deviam existir. (c) **`contentType` não documentado.** O parâmetro `contentType: String? = nil` não tem comentário a explicar que valores são válidos. (d) **`maxResults` default 5 é aplicado APÓS o filtro.** Se há 200 memórias e o filtro retorna 150, o `prefix(5)` corta para 5 — resultados potencialmente não representativos. **Correção:** (1) Usar `minDuration` e `minChars` no filtro (se os campos existirem em `AgentMemory`). Se não existirem, remover os parâmetros da API. (2) Adicionar documentação: `/// - Parameter contentType: Filter by content category. Valid values: "transcript", "summary", "insight", "preference".`. (3) Ordenar resultados por `priority` decrescente + `updatedAt` recente ANTES de aplicar `maxResults`, para que os 5 resultados sejam os mais relevantes. (4) Adicionar parâmetro `sortBy: SortCriterion = .priority` para controlo do caller.

- [ ] **[#46] [P2] `ProviderTemplates` usa `fatalError` em 6 propriedades estáticas (`openAI`, `anthropic`, `gemini`, `openRouter`, `ollama`, `deepSeek`) — se o `ai_config.json` está corrompido ou em falta, aceder a qualquer uma destas propriedades CRASHA a app.** `ProviderTemplates.swift:53-76`:
```swift
static var openAI: ProviderTemplate {
    guard let t = byId["openai"] else { fatalError("OpenAI provider not found in ai_config.json") }
    return t
}
```
**Problemas:** (a) **Crash em produção.** Se `ai_config.json` é acidentalmente excluído do bundle (erro de build) ou corrompido (falha de disco), qualquer acesso a `ProviderTemplates.openAI` crasha a app. (b) **Usado em SwiftUI Previews.** Se o `ai_config.json` não está disponível no target de Previews, as previews crasham. (c) **Usado em `ProviderPickerView` e `ProviderConnectView`.** Se o utilizador abre o Settings → Provider, a view acede a `ProviderTemplates.all` (que itera `byId`) → se um provider esperado está em falta, crash. (d) **Não há fallback.** Mesmo que 5 dos 6 providers estejam presentes, a falta de 1 crasha a app. (e) **`fatalError` em código UI é inaceitável.** Deveria ser um erro recuperável: mostrar "Provider X not available" na UI em vez de crashar. **Correção:** (1) Substituir `fatalError` por `nil` return: `static var openAI: ProviderTemplate? { byId["openai"] }`. (2) Callers devem lidar com nil: `ProviderPickerView` mostra "⚠️ Provider template missing" em vez de crashar. (3) `ProviderTemplates.all` deve ser `byId.values` (todos os que existem), não uma lista fixa de expected providers. (4) Se nenhum template existe, `ProviderPickerView` mostra empty state "No provider templates available. Check ai_config.json." (5) Adicionar teste unitário que verifica que `ai_config.json` contém pelo menos os 6 providers esperados.

- [ ] **[#47] [P2] `AgentMemoryStore.save()` usa `try?` e engole erros de encoding — se as memórias não podem ser codificadas para JSON, a gravação falha silenciosamente e os dados são perdidos.** `ContentPipelineService.swift:1351-1364` — `save()` chama `JSONEncoder.encode(memories)` com `try?`. Se o encoding falha (ex: uma memória contém dados não-Encodable), o erro é completamente ignorado. **Problemas:** (a) **Perda de dados sem notificação.** Se o agente criou 10 memórias novas e o `save()` falha, as memórias existem em memória mas nunca são persistidas. No próximo launch, desapareceram. (b) **Sem log do erro.** `try?` não captura o erro — não há `AppLog` a reportar a falha. O developer nunca sabe que o encoding falhou. (c) **Causa raiz não investigada.** O que faria o encoding falhar? `AgentMemory` é `Codable` — mas se um campo foi adicionado e não é `Codable`, ou se há uma referência circular, o encoding falha. Sem o erro, é impossível diagnosticar. (d) **Inconsistência com outros stores.** `PromptStore.saveOverrides()` usa `try?` também — mas pelo menos loga o erro (`AppLog.provider.error`). `AnalysisSkillStore.saveOverrides()` faz o mesmo. `AgentMemoryStore.save()` é o único que nem loga. **Correção:** (1) Substituir `try?` por `do { try ... } catch { AppLog.agent.error("Failed to save memories: \(error)") }` em TODOS os stores. (2) Em caso de falha de encoding, tentar salvar as memórias individualmente — as que falham são marcadas como `needsRepair` e excluídas do batch. (3) Adicionar `AgentMemoryStore.verifyPersistence()` que, após `save()`, relê o ficheiro e compara com o estado em memória. (4) Emitir notificação `AgentMemoryStore.saveDidFail` para que a UI possa mostrar um banner "Some memories could not be saved."

---

## S — Logging & Debug

- [ ] **[#48] [P2] Operações de configuração não são logadas com structured metadata — não há como rastrear "quem mudou o quê, quando, e porquê" no sistema de configuração.** O `AppLog` (`Logging.swift`) tem categorias (`audio`, `transcription`, `provider`, `storage`, `general`, `agent`) — mas não tem categoria `config`. As operações de configuração usam categorias genéricas ou não são logadas de todo. **Problemas:** (a) **`syncConfigProject` não loga início, fim, ou alterações.** Não há entrada de log quando o sync corre, quantos items foram apagados/criados, ou se houve erros. (b) **Alterações de settings não são auditadas.** Quando `AutomationSettings.shared.autoTranscribe = false`, não há log de "User changed autoTranscribe: true → false". (c) **`PromptStore.addOrUpdate` loga?** Verificar — se não, não há rasto de quando um prompt foi editado. (d) **Provider add/remove não é logado.** `ProviderConnectViewModel.connect()` cria um `AIProviderConfigModel` — sem log estruturado de "Provider 'OpenAI' connected with model 'gpt-5-nano'". (e) **Sem correlation IDs.** Operações de configuração não têm um ID que permita seguir "user abriu Settings → mudou provider → testou conexão → guardou" como uma cadeia de eventos. **Correção:** (1) Adicionar `AppLog.config` categoria dedicada. (2) Logar todas as operações de configuração com structured metadata: `AppLog.config.event("sync_completed", metadata: ["itemsDeleted": 5, "itemsCreated": 5, "duration_ms": 120])`. (3) `AutomationSettings` didSet deve logar mudanças: `AppLog.config.event("setting_changed", metadata: ["key": "autoTranscribe", "oldValue": true, "newValue": false])`. (4) Adicionar `ConfigAuditLog` — um array circular em memória (máx 200 eventos) + persistência em `configs/audit.json` para eventos críticos (provider add/remove, key change). (5) `ConfigAuditLog` visível no config project como item "Audit Log" com timeline dos últimos 50 eventos.

- [ ] **[#49] [P2] Não há vista de diagnóstico/health da configuração — o utilizador não pode ver num só ecrã que items de configuração são válidos, que providers estão saudáveis, ou que erros de configuração existem.** A informação está dispersa: provider health no `ProviderHealthMonitor`, settings no `AutomationSettings`, prompts no `PromptStore`, etc. **Problemas:** (a) **Sem dashboard unificado.** Para diagnosticar "porque é que a análise automática não funciona?", o utilizador tem de verificar: (1) Settings → auto-analyze enabled? (2) Settings → provider ativo? (3) Settings → API key válida? (4) Settings → modelo disponível? (5) Network → conectividade? Cada verificação está num sítio diferente. (b) **Provider health não é exposto na UI.** `ProviderHealthMonitor` mantém `health: [UUID: ProviderHealthStatus]` mas esta informação não é mostrada em lado nenhum exceto nos logs. (c) **Sem diagnóstico proativo.** O sistema não diz "⚠️ Auto-analysis is enabled but no provider with chat capability is active." quando a configuração é inconsistente. (d) **Sem autotest de configuração.** Não há botão "Test Configuration" que verifica: todos os providers respondem? as API keys são válidas? os prompts compilam? as skills têm schema válido? (e) **Config project podia servir como dashboard.** Mas atualmente é um dump de JSON estático — não mostra health, erros, ou warnings. **Correção:** (1) Implementar `ConfigDiagnosticsView` acessível via Settings → "Diagnostics" ou via menu "…" no config project. (2) O dashboard mostra cards: "Providers" (🟢 2 healthy, 🔴 1 error), "Prompts" (✅ 12 loaded, ⚠️ 1 overridden), "Automation" (✅ Auto-transcribe on, ✅ Auto-analyze on with gpt-5-nano), "API Keys" (🔑 2 present, ⚠️ 1 missing), "Network" (📶 Online). (3) Cada card expande para detalhes: erros específicos, sugestões de correção, links diretos para a página de Settings relevante. (4) `ConfigProjectService.buildDiagnostics()` compila todas as verificações num `ConfigHealthReport`. (5) Health report é regenerado a cada launch, a cada alteração de configuração, e on-demand via pull-to-refresh.

---

## T — Resiliency & Edge Cases

- [ ] **[#50] [P0] `ConfigProjectService.createConfigProject()` usa `fatalError` se a criação do config project falha — qualquer problema com SwiftData (corrupção, disco cheio, migração pendente) CRASHA a app no launch, sem recuperação possível.** `ConfigProjectService.swift:40-49` — o método `createConfigProject(context:)` tenta criar um `Project` com slug `wawa-note-config`. Se o `context.save()` falha OU se o fetch subsequente não encontra o projeto, chama `fatalError("ConfigProjectService: failed to create config project: ...")`. **Problemas:** (a) **Crash sem recuperação.** Se a base de dados SwiftData está corrompida, a app crasha no launch — o utilizador nem chega a ver a UI. A única "solução" é apagar a app e reinstalar (perdendo todos os dados). (b) **`fatalError` em produção.** Ao contrário de `assert` ou `precondition` (que podem ser desligados em release), `fatalError` é incondicional — crash garantido. (c) **O config project NÃO é essencial para o funcionamento da app.** A app pode gravar reuniões, fazer transcrição, e gerir projetos sem o config project. O config project é uma conveniência para expor configuração — não devia ser um ponto de falha catastrófico. (d) **Erro de save vs erro de fetch.** O método tenta `context.save()` e depois faz fetch. Se o save falha (ex: validação do modelo), o `fatalError` dispara. Mas se o save funciona e o fetch falha (ex: predicate não faz match), também crasha — apesar de o projeto ter sido criado com sucesso. (e) **`ensureConfigProject` (linha 54) é chamado em `ContentView.onAppear`.** Todas as vezes que a view raiz aparece, o config project é verificado — mas `createConfigProject` só é chamado se o projeto não existe. O `fatalError` só dispara na primeira tentativa de criação — mas se essa tentativa falha, a app nunca arranca. (f) **Sem fallback ou graceful degradation.** Se o config project não pode ser criado, a app podia continuar sem ele — simplesmente não mostraria o config project nas listas de projetos e o utilizador configuraria tudo via Settings. **Correção:** (1) Substituir `fatalError` por `AppLog.config.error("Failed to create config project: ...")` + `return nil` (mudar assinatura para `func createConfigProject(...) -> Project?`). (2) `ensureConfigProject` deve ser `try?` e, se falhar, definir flag `configProjectAvailable = false`. (3) Views que dependem do config project (`InboxView`, `ExploreView`, `KnowledgeDetailView`) devem verificar `configProjectAvailable` e esconder/mostrar conteúdo condicionalmente. (4) Adicionar botão "Retry Config Project" no Settings → Advanced para tentar criar o config project manualmente após um erro. (5) `syncConfigProject` deve verificar `configProjectAvailable` antes de correr e simplesmente retornar se false. (6) Adicionar `ConfigProjectRecoveryService` que tenta reparar o config project: recriar com UUID diferente, fazer vacuum ao SwiftData, ou fazer reset do config project mantendo os dados.












